# Gate: T-20260821-005 — aplicar migración de datos legales/salud en `members`

- severity: high
- approval_mode: sync (bloqueante — no ejecutar sin aprobación explícita acá)
- requires_named_approver: true
- created: 2026-08-21

## Qué intentó

Ya se hizo, es local y no toca la base real (revertible con `git restore`/`git clean`):

- `db/schema/members.ts` — 4 columnas nuevas: `birth_date` (date, nullable), `dni` (text, nullable), `medical_certificate_submitted` (boolean, `NOT NULL DEFAULT false`), `health_notes` (text, nullable).
- `lib/validations/member.schema.ts` — validación de DNI (7-8 dígitos, Argentina) y normalización de los campos opcionales nuevos.
- `app/api/v1/members/route.ts` + `[id]/route.ts` — POST/PATCH aceptan los 4 campos nuevos. Sin cambios de permisos: sigue siendo `["owner","staff"]` en todo, igual que el resto de `members` (decisión del usuario 2026-08-21: staff también ve los datos de salud, mismo acceso que el resto de la ficha).
- `features/members-page/components/member-form-dialog.tsx` (+ `.module.css`) — campos de alta/edición: fecha de nacimiento, DNI, notas de salud (textarea), checkbox "Presentó el certificado médico".
- `features/member-detail-page/index.tsx` — muestra los 4 campos en la ficha del socio (certificado médico como `StatusPill` success/alert).
- `db/migrations/0005_modern_nick_fury.sql` — generado con `drizzle-kit generate` (sin conexión a DB, solo diff de schema local). Puramente aditivo: 4 `ALTER TABLE members ADD COLUMN`, ninguna columna existente tocada.
- `npx tsc --noEmit`, `npx eslint` y el detector mecánico de `impeccable` sobre las páginas tocadas: todos limpios.

Falta ejecutar: `npm run db:migrate` (corre `drizzle-kit migrate` contra `DATABASE_TRUSTED_URL` — el Supabase real del proyecto — y después `scripts/apply-policies.ts`, que reaplica los 3 archivos de `db/policies/` en orden; no agrega ninguno nuevo, `members` ya tenía RLS desde antes).

## SQL que se aplicaría

```sql
ALTER TABLE "members" ADD COLUMN "birth_date" date;
ALTER TABLE "members" ADD COLUMN "dni" text;
ALTER TABLE "members" ADD COLUMN "medical_certificate_submitted" boolean DEFAULT false NOT NULL;
ALTER TABLE "members" ADD COLUMN "health_notes" text;
```

## Por qué requiere revisión

- Migración de schema contra la base de datos real del proyecto (mismo motivo que T-20260821-003).
- `health_notes` es un dato sensible bajo la Ley 25.326 (Argentina) — se decidió con el usuario (2026-08-21) que owner y staff tienen el mismo acceso, sin restricción adicional a nivel RLS. Queda registrado acá para que quien apruebe sepa que esa decisión ya se tomó explícitamente, no que se pasó por alto.
- Es aditivo — no borra ni modifica ninguna columna, tabla o política existente. `medical_certificate_submitted NOT NULL DEFAULT false` no requiere backfill: el default cubre las filas existentes automáticamente.

## Aprobación

- [x] Aprobado por: SudacaDev (git user de la sesión; aprobación explícita en conversación: "apruebo npm run db:migrate?")
- [x] Fecha: 2026-08-21
- Comando a correr una vez aprobado: `npm run db:migrate`

**Nota de proceso:** igual que T-20260821-003, este archivo se deja resuelto en `gates/pending/` en vez de moverse a `gates/approved/` — invariante de `roles/registry.yml` (ningún rol automatizado escribe ahí).
