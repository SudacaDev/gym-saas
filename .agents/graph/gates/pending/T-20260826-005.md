# Gate: T-20260826-005 — Skeleton en la grilla de Planes (`plans-page`)

- severity: medium (`edit_non_prod_file`, UI pura sin tocar base de datos)
- approval_mode: async
- created: 2026-08-26

## Qué se hizo

`features/plans-page/index.tsx` — la rama `loading` del condicional (antes `<p>Cargando...</p>`) ahora renderiza 3 `<PlanCardSkeleton />` dentro del mismo `styles.grid` que usa la vista real. El header (`Planes` + botón "Nuevo plan") no cambió — ya se renderizaba siempre, sin depender de `plans`.

- `features/plans-page/components/plan-card-skeleton.tsx` (nuevo) + `.module.css` (nuevo) — reusa `pageStyles.card` (el contenedor real de la card, con su radio ya cappeado) y reemplaza por `<Skeleton>` (`components/ui/skeleton.tsx`, creado en T-20260826-004, no se recreó) los 4 elementos data-dependientes: `periodTag`, `cardName`, `cardPrice`, los 2 botones de `cardActions`.

## Por qué requiere revisión

- Cambia el estado de carga de una pantalla real del producto — mismo criterio de riesgo bajo que T-20260826-004 (visual puro, sin datos ni permisos).
- Segunda vez que se aplica este patrón (después de `member-detail-page`) — confirma que el primitivo `Skeleton` genérico es reusable entre features sin fricción.

## Verificación

- `npx tsc --noEmit`: limpio.
- `npx eslint features/plans-page`: limpio.
- Detector mecánico de `impeccable` sobre `features/plans-page`: limpio, exit 0.
- `npm run test:unit`: 23/34 verdes. **5 archivos fallaron por un error de conexión a la base** (`checkin-checkout`, `checkin-requires-active-membership`, `dashboard-metrics`, `member-soft-delete`, `payment-extends-membership`) — ninguno importa ni ejercita `plans-page` ni ningún archivo tocado por esta tarea. Mismo patrón ambiental de fallas de DB documentado repetidamente en sesiones anteriores (antes como `PostgresError 53300` de pool agotado; esta vez el síntoma fue un `TypeError` en el `afterAll` de `payment-extends-membership.test.ts` al intentar borrar un tenant que nunca se creó — consistente con que el `beforeAll` de esos tests falló contra la base antes de llegar siquiera al test). No investigado más a fondo por ser un problema de entorno de test, no de este cambio.
- `reducto context` post-tarea: 83% de tokens ahorrados en la sesión (segunda tarea consecutiva confirmando T-20260826-003).

## Gaps conocidos

- Sin verificación visual en navegador (mismo gap estructural de sesiones anteriores).
- El `TypeError` de los 5 tests de integración no se investigó — si se repite en la próxima sesión, vale la pena revisar la config de conexión a la base de test (`.env.test`/pool), no solo asumir que es el mismo `PostgresError 53300` de siempre sin confirmarlo.
- No se reindexó el grafo (`knowledge/`) tras los 2 archivos nuevos.

## Aprobación

- [ ] Aprobado por:
- [ ] Fecha:
- Sin comando post-aprobación pendiente — no hay migración ni cambio de datos reales en esta tarea.
