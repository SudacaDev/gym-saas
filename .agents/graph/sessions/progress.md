# Progress

> Este archivo es la memoria persistente entre sesiones (principio P).
> Cada sesión de trabajo debe actualizar esto antes de cerrar — no confiar
> en que el contexto de la conversación sobreviva.

## Estado actual
- Instalación: brownfield — completada el 2026-08-20
- Indexación inicial: completa (55 nodos / 42 edges / 7 comunidades al instalar)
- Reindexación más reciente: 2026-08-25 (`/graph-ia:graph --reindex`) — 190 nodos / 100 edges / 9 comunidades. Al día — incluye T-004 a T-009 y T-20260824-001/-002/-003.
- Reconciliación de historial (solo brownfield): importado vía git log — ver `graph/history/pre-graph-commits.md`
- Backlog activo: 0 en curso, 0 salteadas, 16 completadas (T-20260821-001 a -009, T-20260824-001, -002, -003, T-20260825-001 a -004). Pendientes en `tasks.md`: T-20260825-005 (fichaje, bloqueada por open_questions y por `depends_on: T-002`), T-20260825-006 (vistas de Horarios, bloqueada por open_questions).
- Última ejecución de `#run`: `#run-all` el 2026-08-25 — resueltas todas las open_questions de T-20260825-001 a -004 vía `AskUserQuestion` (varias rondas), construidas las 4 vía subagentes (`backend-developer`/`frontend-developer`), un `#task` nuevo (T-20260825-006) recibido y registrado a mitad de la corrida.
- **Circuit breaker:** se disparó dos veces durante T-20260821-004 (detalle completo más abajo, sin repetirse en el resto de la sesión, incluida esta).
- **Gates síncronos/async pendientes de aprobación (2026-08-25):**
  - `T-20260825-002.md` — **`high`, síncrono, requiere aprobador nombrado** — reemplaza el flujo de invitación por email de `staff` por uno donde el owner define usuario+contraseña directamente. Bloquea `npm run db:migrate` de la migración `0012`. El gate señala confirmar que `staff_members` tiene 0 filas reales en producción antes de aprobar (la columna `username` se agrega `NOT NULL`).
  - `T-20260825-001.md` — `medium`, async — presencia en tiempo real + `GET /api/v1/staff` owner-only. Sin migración (no bloquea nada, es código ya commiteado).
  - `T-20260825-003.md` — `medium`, async — `members.short_code`. Bloquea `npm run db:migrate` de la migración `0010`.
  - `T-20260825-004.md` — `medium`, async — `members.checkin_code` + auto-check-in. Bloquea `npm run db:migrate` de la migración `0011`.
  - Las migraciones `0010`→`0011`→`0012` son secuenciales (mismo `drizzle-kit migrate`) — se aplican todas juntas en un solo `npm run db:migrate` una vez que el gate `high` de T-002 esté aprobado (los `medium` son async y no bloquean per se, pero comparten el mismo comando de aplicación que el `high`, así que en la práctica hay que esperar la aprobación de T-002 para correr cualquiera de las tres).
- **Pendiente para que los recordatorios manden mail de verdad en producción (no es código, es config de deploy):** configurar `CRON_SECRET` en Vercel y verificar un dominio propio en Resend si todavía no está (mismo requisito que ya tenía el recibo de pago).

## Última sesión
- Fecha: 2026-08-21
- Qué se hizo:
  - Instalación de GRAPH en modo brownfield con `--migrate` (sin archivos previos que migrar).
  - Ajuste de contenedor en `app/(owner)/layout.tsx`: `max-w-4xl` → `max-w-[1580px]`.
  - Migración de clases Tailwind inline (`className="..."`) a archivos `*.module.css` colocados junto a cada componente bajo `app/**` (~19 archivos), usando `@apply` + `@reference "<ruta a globals.css>"`. Alcance acordado con el usuario: `components/ui/*` (primitivos shadcn con `cva`) quedaron fuera intencionalmente.
  - Extracción de los mapas de clases dinámicas (`STATUS_CLASSES`, `PAYMENT_STATUS_CLASSES` en `checkin/page.tsx` y `members/[memberId]/page.tsx`) para que referencien clases de CSS module (`styles.statusActive`, etc.) en vez de strings Tailwind crudos.
  - Reindexación del grafo tras la migración (`--reindex`): 122 nodos / 67 edges / 7 comunidades.
  - `/impeccable document`: generado `DESIGN.md` (+ sidecar `.impeccable/design.json`) capturando el sistema visual incumbente ("The Box Ledger" — dark-only, un acento naranja, flat-by-default, Archivo Black uppercase). No existía `DESIGN.md` previo.
  - Sección de planes (`features/plans-page`): tabla → grilla de cards (T-20260821-002 — ver nota de proceso, se ejecutó sin pasar por `#run` ni por el gate `medium` que le correspondía).
  - `#task` nuevo del usuario: horarios semanales. Registrado como dos tareas encadenadas (T-20260821-003 schema, T-20260821-004 UI, 004 depende de 003) — esta vez SÍ se registró y esperó, sin ejecutar nada hasta que el usuario mandó `#run-all`.
  - Antes de definir T-20260821-004, se convocó en paralelo a `marketing-director` y `uiux-designer` (a pedido explícito del usuario, que delegó esa decisión) para resolver "sección Horarios propia vs. panel en dashboard". Ambos coincidieron, independientemente, en sección propia — razonamiento completo en `tasks.md`.
  - `#run-all`: ejecutó T-20260821-003 hasta el límite de lo local — `db/schema/enums.ts` (+`dayOfWeekEnum`), `db/schema/class-schedules.ts` (tabla nueva, scope v1 estrictamente declarativo: día/hora/actividad, sin cupo/instructor/reservas por decisión de marketing), `db/schema/relations.ts`, `db/schema/index.ts`, `db/policies/0003_class_schedules_rls.sql` (mismo patrón que las 6 tablas existentes), migración generada con `npm run db:generate` (`db/migrations/0004_nappy_mattie_franklin.sql`, sin tocar la DB real), y extensión de `tests/integration/tenant-isolation/rls-policies.test.ts` para cubrir la tabla nueva. `tsc --noEmit` limpio en todo. Frenó ahí: aplicar la migración es `high` severity, así que se abrió gate síncrono en `graph/gates/pending/T-20260821-003.md` en vez de correr `npm run db:migrate`.
  - Aprobación síncrona del gate `high`: usuario ("SudacaDev") aprobó explícitamente correr la migración. `npm run db:migrate` ejecutado y verificado contra la base real (`class_schedules`: 8 columnas, `relrowsecurity`/`relforcerowsecurity` en `true`, policy `tenant_isolation` presente). T-20260821-003 pasa a completada.
  - Usuario preguntó por el estado de T-20260821-002 (visto en `tasks.md` abierto en el IDE) y pidió explícitamente revertirla y re-correrla por el flujo correcto. Se revirtió `features/plans-page/index.tsx`/`index.module.css` a la tabla original, se registró el gate async correspondiente (`graph/gates/pending/T-20260821-002-redo.md`, severidad `medium`), y se reimplementó la grilla de cards — mismo resultado, esta vez con rastro de proceso completo.
  - Nuevo `#task` del usuario mientras tanto: agregar a la tabla de socios fecha de cumpleaños, DNI y datos legales/de salud (certificado médico, problemas de salud). Registrado como **T-20260821-005**, sin ejecutar — incluye flag de dato sensible (Ley 25.326) como open_question a resolver antes de implementar.
  - Se retomó T-20260821-004 (UI de horarios): exploración de convenciones de rutas/API/formularios existentes (`app/(owner)/plans/page.tsx`, `app/api/v1/plans/**`, `lib/auth/require-role.ts`, `plan-form-dialog.tsx`) para replicar el mismo patrón en horarios.
  - **Primer corte del circuit breaker:** a mitad de la exploración de convenciones (varias lecturas seguidas sin editar ningún archivo), se disparó `stagnation` (`consecutive_no_progress: 20`, señal `git_diff_hash_unchanged`) — bloqueó Bash y Edit vía hook `PreToolUse`. Se reportó al usuario con el detalle completo (incluyendo que el propio `circuit-breaker.yml` documenta esta señal como propensa a falsos positivos en secuencias de lectura/verificación legítimas) y se esperó su revisión, sin intentar bypasear el marcador `.circuit-breaker-tripped`. El usuario respondió "podes seguir"; el marcador ya no existía en el siguiente intento y las herramientas volvieron a funcionar — no quedó claro por qué mecanismo exacto se limpió (no hay adaptador visible en el repo, `enforcement/claude-code-hook.sh` vive fuera de `.agents/`, en `${CLAUDE_PLUGIN_ROOT}`), documentado acá como comportamiento observado, no verificado en código.
  - Se construyó T-20260821-004 completa: `lib/validations/schedule.schema.ts`, `app/api/v1/schedules/route.ts` + `[id]/route.ts` (permisos `["owner","staff"]` en las 4 operaciones, a diferencia de `plans` que restringe escritura a `owner` — decisión explícita del usuario), `features/schedules-page/` (grid semanal 7 columnas hairline, patrón `benefits`-grid de `DESIGN.md`, colapsa en mobile), `app/(owner)/schedules/page.tsx`, link "Horarios" en el nav. `tsc`/`eslint` limpios.
  - Corrido el detector mecánico de `impeccable` (paso obligatorio del skill tras UI) sobre `schedules-page` y `plans-page`: encontró un advisory (`10px` fuera de la escala tipográfica en `plans-page`, no relacionado a T-004 pero corregido de paso a `11px`, el tamaño Label documentado). Segunda corrida: limpio.
  - **Segundo corte del circuit breaker:** al final, una racha de comandos de verificación (`tsc`, `eslint`, detector dos veces, `tsc` final) que no tocan el working tree entre sí volvió a disparar `stagnation`. Mismo protocolo: se reportó, no se bypaseó, se esperó revisión. El usuario abrió `circuit-breaker.yml` en el editor y respondió "podes seguir" de nuevo.
  - Mientras el segundo corte estaba activo, el usuario mandó dos `#task` nuevos. Se capturó su contenido en la respuesta al usuario (sin persistir, ya que Edit estaba bloqueado) y se registraron recién después de la segunda luz verde: **T-20260821-006** (ocultar "ingreso del mes" a staff) y **T-20260821-007** (alta real de staff con formulario especializado por categoría — profesores/administrativos/limpieza). Ninguna de las dos se ejecutó.
  - `#run T-20260821-005`: antes de tocar el schema se resolvieron las dos open_questions con el usuario (`AskUserQuestion`) — columnas directo en `members` (no tabla separada) y mismo acceso para owner/staff sobre el dato de salud (sin restricción adicional). Se construyó: `db/schema/members.ts` (+4 columnas: `birth_date`, `dni`, `medical_certificate_submitted`, `health_notes`), validación de DNI argentino (7-8 dígitos) en `lib/validations/member.schema.ts`, API `members` actualizada, campos nuevos en `member-form-dialog.tsx` (fecha, DNI, textarea de salud, checkbox de certificado), y la ficha de `member-detail-page` mostrando los 4 campos (certificado como `StatusPill`). `tsc`/`eslint`/detector de `impeccable` limpios. Migración generada (`db/migrations/0005_modern_nick_fury.sql`, puramente aditiva), gate `high` abierto en `graph/gates/pending/T-20260821-005.md`, aprobado por el usuario ("apruebo npm run db:migrate?"), `npm run db:migrate` corrido y verificado contra la base real (4 columnas confirmadas con tipo/nullability correctos vía script de verificación descartable).
  - Durante T-20260821-005 se notó (no se tocó en el momento) que `features/schedules-page/` cambió en disco fuera de esta sesión — el usuario evolucionó la grilla semanal a una matriz real día×hora (filas de horario, columnas de día, headers sticky). Quedó terminada y compilando por su cuenta (confirmado más tarde vía `tsc`).
  - El usuario agregó directo a `tasks.md` (fuera de mi flujo de escritura, visto vía system reminder de "archivo cambiado en disco") dos tareas más, con el mismo formato de registro: **T-20260821-008** (catálogo de actividades) y **T-20260821-009** (chips de día también en "Editar" horario). Tratadas como legítimas, sin revertir.
  - `#run-all`: recorrido en orden T-006 → T-007 → T-008 → T-009.
    - **T-006:** implementada con el default declarado (ocultar todo el bloque de ingresos — label/valor/tendencia — para `role !== "owner"`, dejar el resto del hero igual). La segunda open_question (qué mostrar en el lugar vacío) se resolvió por decisión propia: nada, sin placeholder. Cambio de una línea en `features/dashboard-page/index.tsx` (wrap condicional), sin tocar `metrics.ts` (Server Component, el dato no renderizado no llega al cliente igual). `tsc`/`eslint`/detector limpios (6 advisories de tamaño de fuente preexistentes en ese módulo, no relacionados, no tocados).
    - **T-007:** salteada — sigue bloqueada por falta de los campos por categoría, severidad `high`. Movida a "Tareas salteadas (con motivo)" en `tasks.md`, no se inventó nada.
    - **T-008:** antes de tocar el schema se resolvieron con `AskUserQuestion` las dos decisiones que cambiaban su forma — quick-add dentro del diálogo de horario (no página propia) y FK real `activity_id` (no solo autocompletar). El borrado de actividad en uso se decidió por precedente propio del código (mismo patrón `ON DELETE RESTRICT` + 409 que `plans`→`memberships`), sin pregunta nueva. Antes de escribir la migración se verificó contra la base real que `class_schedules` ya tenía 6 filas reales con `activity_name` ("Localizada", "Funcional" ×2, "PowerFit" ×3) — no era una tabla vacía, así que la migración necesitaba backfill de datos, no solo estructura. Se resolvió en dos pasos para evitar la heurística de detección de rename de `drizzle-kit` (que se dispara en modo interactivo si un mismo diff agrega y quita columnas parecidas): **pass A** (hecho, gate `high` abierto en `graph/gates/pending/T-20260821-008-pass-a.md`, sin aprobar todavía) agrega `activities` + `activity_id` nullable + backfillea las 3 actividades distintas y reasigna las 6 filas existentes, con SQL de backfill agregado a mano después de la parte generada por `drizzle-kit generate`; **pass B** (no empezado) hará `activity_id NOT NULL` + drop de `activity_name` + el rework de UI/API, en un gate `high` separado una vez que A esté aplicado y verificado.
    - **T-009:** unificado el picker de días de `schedule-form-dialog.tsx` — "Editar" pasó del `Select` de un solo día a los mismos chips multi-día que "Nuevo horario", pre-tildando el día original. `onSubmit` diffea contra ese día: se mantiene → `PATCH`; se destilda → `DELETE` sin `confirm()` extra (default confirmado por el usuario) vía el callback nuevo `onRemoved`; día nuevo tildado → `POST` con los mismos valores del formulario (segundo default confirmado). `onRemoved` wireado en `SchedulesPage`. Imports no usados limpiados. `tsc`/`eslint`/detector limpios.
- Tareas completadas:
  - [x] T-20260821-001 — Migrar clases Tailwind inline a CSS por componente bajo `app/**`
  - [x] T-20260821-002 — Planes: tabla → grilla de cards (redo con proceso completo tras el desvío de la sesión anterior)
  - [x] T-20260821-003 — Horarios: schema + RLS + migración aplicada y verificada contra la base real
  - [x] T-20260821-004 — Horarios: UI completa
  - [x] T-20260821-005 — Socios: cumpleaños/DNI/certificado médico/notas de salud, schema + UI + migración aplicada y verificada
  - [x] T-20260821-006 — Dashboard: ingreso del mes oculto para staff
  - [x] T-20260821-009 — Horarios: chips de día unificados entre Nuevo y Editar
- Tareas en curso:
  - [ ] T-20260821-008 — Catálogo de actividades: pass A hecho, frenado en gate `high` (`graph/gates/pending/T-20260821-008-pass-a.md`) esperando aprobación síncrona antes de `npm run db:migrate`. Pass B (UI + API + drop de `activity_name`) no empezado.
- Tareas salteadas:
  - [~] T-20260821-007 — Alta real de staff + formulario por categoría — sigue bloqueada por falta de los campos por categoría (profesores/administrativos/limpieza). Severidad `high` (crea cuentas reales de Supabase Auth).
- Tareas pendientes que siguen en `tasks.md`:
  - [ ] circuit-breaker.yml revisado y ajustado a este proyecto
  - [ ] policy.yml revisado
  - [ ] roles/registry.yml — qué roles se activan en este proyecto
- Por qué se cortó por circuit breaker (dos veces, ambas en T-20260821-004): ver sesión anterior — no volvió a dispararse en el resto de esta sesión (T-005 en adelante).
- **Desvío de proceso (corregido, sesión anterior):** T-20260821-002 se ejecutó fuera de proceso una vez y se corrigió con revert + redo formal. Desde entonces se respetó el flujo `#task` → registrar y esperar → gate si aplica → `#run`/`#run-all` en T-003 a T-009.

## Sesión 2026-08-24
- Usuario compartió 7 capturas de Linear pidiendo que gym-saas "se vea más a un SaaS, más profesional". Análisis propio: la brecha más grande es de app-shell (nav horizontal tipo landing) no de paleta/tipografía (`DESIGN.md` ya resuelto). Se sugirieron 4 cambios; el usuario acordó arrancar solo por el sidebar (T-20260824-001), los otros 3 quedaron anotados en `tasks.md` como sugeridos sin pedir.
- `#task` registrado y `#run T-20260824-001` recibido en el mismo turno. Severidad confirmada contra `gates/policy.yml` (`edit_non_prod_file` → `medium`, async) antes de tocar código — quedaba pendiente esa confirmación desde que se registró la tarea. Gate async abierto en `graph/gates/pending/T-20260824-001.md`.
- Implementado: `app/(owner)/owner-nav.tsx` + `.module.css` pasaron de nav horizontal a sidebar vertical fijo (`sticky top-0 h-screen`) con brand arriba, un grupo "Gestión" y 5 links icono+label (`lucide-react`, ya era dependencia — `LayoutDashboardIcon`, `UsersIcon`, `CreditCardIcon`, `CalendarDaysIcon`, `ScanLineIcon`). Estado activo: borde izquierdo 2px Ignition Orange + fill `--accent` (único lugar del sistema donde el nav lleva background, antes no lo tenía). Colapsa a rail icon-only (sin label, sin JS/toggle) por debajo de `md` para no romper el check-in en mobile/tablet, que `DESIGN.md` documenta como dispositivo primario.
- `app/(owner)/layout.tsx` + `.module.css`: de columna (`header` arriba + `main` abajo) a fila (sidebar + `main`, con `min-w-0` en `main` para no desbordar con contenido ancho tipo tablas).
- `DESIGN.md` sección "Navigation" actualizada para reflejar el patrón nuevo (antes describía el underline horizontal, ahora describe el sidebar + su estado activo) — se consideró parte de la tarea, no scope creep, porque dejar esa sección desactualizada rompe G (grounded) para cualquier agente que la consulte después.
- `tsc --noEmit` y `eslint` limpios. No se verificó visualmente en navegador (requiere sesión real contra el Supabase del proyecto).
- No se tocaron las páginas individuales bajo `app/(owner)/**` — ninguna asumía el nav horizontal de forma que rompiera con el cambio (grep de `sticky`/`z-`/`position: fixed` bajo `app/(owner)/**` no encontró nada en conflicto).
- Los otros 3 ítems sugeridos (selector de negocio/tenant, empty states con ilustración+CTA, configuración agrupada en cards) siguen sin pedirse — no se tocaron.

## Sesión 2026-08-24 (cont.) — `#run T-20260821-008` (pass B)
- Usuario mandó `#run T-20260821-008` para retomar el pass B pendiente (picker de actividades). Se construyó todo el código local, generó la migración con `drizzle-kit generate` (sin tocar la base real) y se frenó antes de aplicarla — severidad `high` por precedente directo (mismo patrón que T-003/T-005/pass-A de este mismo T-008).
- `db/schema/class-schedules.ts`: `activityId` → `NOT NULL`, columna `activityName` eliminada. Migración generada (`db/migrations/0007_many_silver_surfer.sql`) es exactamente `ALTER COLUMN activity_id SET NOT NULL` + `DROP COLUMN activity_name` — sin backfill propio porque pass A ya dejó las 6 filas reales con `activity_id` asignado.
- `app/api/v1/activities/route.ts` (nuevo): `GET` lista el catálogo; `POST` es get-or-create atómico vía `onConflictDoUpdate` sobre `(tenant_id, name)` — se descartó un select-then-insert en dos pasos porque `withTenantContext` corre todo en una transacción real: un insert fallido por unicidad la deja abortada y cualquier query siguiente en la misma transacción también falla.
- `lib/validations/activity.schema.ts` (nuevo) y `schedule.schema.ts` (`activityId` uuid reemplaza `activityName` texto libre). `app/api/v1/schedules/route.ts` + `[id]/route.ts` actualizados a juego.
- `schedule-form-dialog.tsx`: el `Input` de texto libre para actividad pasó a un `Select` (mismo patrón `Controller` que `plan-form-dialog.tsx`) poblado con el catálogo, más un ítem "+ Nueva actividad" que revela un `Input` inline y crea la actividad sin cerrar el diálogo (POST a `/api/v1/activities`, get-or-create — si el operador escribe un nombre que ya existe, se reusa en vez de fallar).
- `schedules-page/index.tsx`: ahora carga el catálogo junto con los horarios y resuelve el nombre a mostrar vía un `Map<id, name>` (antes venía embebido en cada fila).
- `tests/integration/tenant-isolation/rls-policies.test.ts`: el seed de `class_schedules` ya no manda `activityName` (columna eliminada del schema).
- `npx tsc --noEmit`, `npx eslint .` y `npm run test:unit` (28/28) limpios. Suite de integración (`test:integration`) no corrida — requiere `DATABASE_URL` real y, en este caso, la migración ya aplicada, que es justo lo que está pendiente de aprobación.
- Gate síncrono `graph/gates/pending/T-20260821-008-pass-b.md` **aprobado** por el usuario vía `AskUserQuestion` ("Sí, correr la migración"). Primer `npm run db:migrate` falló antes de ejecutar ningún DDL propio: `PostgresError 53300` (pool de conexiones saturado). Base sin tocar — no se reintentó de inmediato para no seguir presionando el pool.
- El usuario pidió reintentar ("correlo vos al migrate") mientras, en paralelo, también lo corría en su propia terminal — se le avisó de no correrlo desde los dos lados a la vez. Segundo intento desde acá: la conexión ya no falló, pero apareció un error distinto y real: `PostgresError 23502` ("column activity_id ... contains null values"). El CLI de `drizzle-kit` seguía sin mostrar el error de fondo (spinner lo tapaba), así que se bypaseó corriendo `drizzle-orm/postgres-js/migrator` directo.
- Un `SELECT` de solo lectura confirmó la causa: **4 filas reales** de `class_schedules` (`activity_name = 'indoor cycling'`, creadas 2026-08-22T16:29:58Z) tenían `activity_id NULL` — se habían cargado, después de la verificación de pass A, con la API vieja que todavía no tocaba `activity_id`. No era un dato inventado por ningún gate anterior; el estado de la base simplemente avanzó porque la app se siguió usando entre pass A y pass B.
- Reportado al usuario con el detalle exacto (4 filas, fecha, causa) y una propuesta de backfill puntual (misma lógica que el backfill de pass A, acotada a esas 4 filas con `WHERE activity_id IS NULL`). Aprobado vía `AskUserQuestion` ("Sí, backfill + migrar").
- **Backfill ejecutado** en una transacción: 1 fila nueva en `activities` ("indoor cycling"), 4 filas de `class_schedules` actualizadas. Verificado: 0 filas `activity_id NULL` restantes.
- **Migración aplicada** (`activity_id NOT NULL` + drop de `activity_name`) + `apply-policies.ts` (4 archivos, sin cambios, idempotente).
- **Verificado contra la base real:** `class_schedules` con 8 columnas (`activity_name` ya no existe, `activity_id NOT NULL`), las 10 filas reales joinean correctamente contra `activities`, RLS forzada en ambas tablas. T-20260821-008 (pass A + pass B) queda **completa**.
- Los tres scripts de diagnóstico/backfill (`scripts/_diag-*.ts`) fueron disposables — creados y borrados dentro de esta sesión, nunca commiteados. Detalle completo del incidente y su resolución en `graph/gates/pending/T-20260821-008-pass-b.md`.

## Sesión 2026-08-24 (cont.) — `#run-all`
- Usuario mandó `#run-all`. Tres tareas pendientes en `tasks.md` (T-20260824-002, T-20260821-007, T-20260824-003) tenían preguntas abiertas explícitamente marcadas "no asumir, preguntar antes de `#run`" — se resolvieron las 4 relevantes vía `AskUserQuestion` antes de tocar código: T-002 = "Schema primero" (no mock), T-007 = confirmado el set de campos tal cual, y dos preguntas de Gmail (vencimiento de membership / Calendar afuera de v1) que quedaron sin usar porque, al releer el estado en disco, T-20260824-003 ya estaba completa — construida por otra sesión/ventana en paralelo mientras se hacían las preguntas (pivotó de OAuth de Gmail a reusar Resend, ya integrado en el proyecto; decisión sensata tomada con el usuario en esa otra sesión). No se rehizo nada de Gmail, se tomó el estado en disco como autoritativo.
- Se avisó al usuario del hallazgo (T-003 ya completa) y se pasó a construir T-20260821-007 + T-20260824-002 juntas, dado que la segunda quedó explícitamente dependiente de la primera por la respuesta "Schema primero".
- **T-20260821-007 (schema + alta real):** `db/schema/enums.ts` (+3 enums de staff), `db/schema/staff-members.ts` (nuevo — soft delete vía `deletedAt`, ajustado a la convención real de `members.ts` en vez del boolean `active` que sugería el plan original), `relations.ts`/`index.ts`, `db/policies/0006_staff_members_rls.sql`, migración generada `db/migrations/0009_cheerful_emma_frost.sql` (puramente aditiva, sin backfill). `lib/validations/staff-member.schema.ts` (alta con `superRefine` para exigir especialidades solo si `staffCategory === "instructor"`; edición sin `email`). `app/api/v1/staff/route.ts` (GET owner+staff; POST owner-only — `admin.auth.admin.inviteUserByEmail` + inserción transaccional de `users`+`staff_members`, con compensación explícita: si la transacción falla, se borra la cuenta de Auth recién creada para no dejarla huérfana) y `[id]/route.ts` (PATCH/DELETE owner-only, DELETE es soft delete).
- **T-20260824-002 (pantalla Equipo):** `features/staff-page/` completo (tabla + buscador cliente + `StaffFormDialog` con campos condicionales por categoría, reusando el patrón de chips de `schedule-form-dialog.tsx` para especialidades), `app/(owner)/staff/page.tsx`, link "Equipo" en `owner-nav.tsx` (`IdCardIcon`). "Última conexión" de la referencia de Linear quedó explícitamente afuera — vive en `auth.users.last_sign_in_at`, no modelado en Drizzle; traerla por fila implicaría N llamadas a la Admin API por request.
- `npx tsc --noEmit`, `npx eslint .` y `npm run test:unit` (34/34) limpios.
- Gate `high` abierto en `graph/gates/pending/T-20260821-007.md` (cubre ambas tareas, comparten la misma migración) — **sin aprobar todavía**, `npm run db:migrate` no corrido.
- Gaps conocidos documentados en el gate: sin extender el test de aislamiento RLS para `staff_members` (mismo gap que quedó en `email_send_log` de T-003), sin revocación real de acceso de Auth en el soft-delete, sin verificación visual en navegador.
- Usuario aprobó el gate (`AskUserQuestion`, "Sí, correr la migración"). `npm run db:migrate` corrido **sin incidentes** (a diferencia de T-20260821-008: sin problemas de conexión, sin datos huérfanos). Verificado contra la base real: `staff_members` con 17 columnas, tipos/nullability correctos, RLS forzada, policy `tenant_isolation` presente. T-20260821-007 y T-20260824-002 pasan a completadas.
- Fin de la racha de `#run-all` de esta sesión: backlog activo queda en 0 tareas en curso, 10 completadas hoy, el grafo de conocimiento sigue pendiente de reindexar (ver "Estado actual"). Ninguna cuenta real de staff fue creada — solo ocurre cuando alguien use "Invitar" en la app en producción.

## Sesión 2026-08-25 — `/graph-ia:graph --reindex`
- Usuario corrió `/graph-ia:graph --reindex` (sin `--mode`, refresh standalone). Ejecutado `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/build-graph.py <repo_root>` (el indexador propio del plugin, regex-based, sin dependencias de terceros) tal como indica `commands/graph-ia.md` Paso 7.
- Resultado: 190 nodos / 100 edges / 9 comunidades (antes: 122/67/7 al 2026-08-21) — `knowledge/index.json` sobreescrito con estos conteos reales, `last_indexed: 2026-08-25T14:45:25Z`.
- No se re-corrió la reconciliación de historial de git: `.agents/graph/history/pre-graph-commits.md` ya existía (marcador de que ya se había hecho), y el paso 7 explícitamente skipea ese re-run cuando ese archivo está presente — `--reindex` solo fuerza el índice de código, no el historial.
- Con esto, el grafo queda al día con todo lo construido en la sesión anterior (T-20260821-004 a -009, T-20260824-001/-002/-003) — ya no hay deuda de reindexación pendiente.

## Sesión 2026-08-25 (cont.) — 4×`#task`
- Usuario mandó 4 `#task` en un mismo mensaje. Registrados en `tasks.md` (T-20260825-001 a -004), **sin implementar nada** — flujo `#task` = solo registrar y esperar `#run`.
- Cada una quedó con sus open_questions explícitas, sin adivinar ninguna:
  - **T-001** (header de sesión + nav Equipo owner-only + ingreso mensual): "ingreso mensual solo owner" ya está hecho desde T-20260821-006 — señalado como posible duplicado/recordatorio, no reimplementado a ciegas. "Quién está conectado" se interpretó tentativamente como identidad+logout del usuario actual (no presence tracking del equipo), pero queda como pregunta a confirmar. Restringir "Equipo" a owner-only tensiona con el permiso `["owner","staff"]` en `GET /api/v1/staff` que se definió en T-007/T-002 — señalado, no resuelto.
  - **T-002** (usuario/contraseña + horario de entrada/salida + 2FA a futuro): marcada como tensión directa con el diseño de T-20260821-007 (invite-by-email, el owner nunca ve la contraseña) — el pedido de que el owner cargue usuario+contraseña directamente es un cambio de arquitectura de auth, no un agregado incremental. "Horario de entrada/salida" se señaló como una feature de asistencia/fichado bastante más grande de lo que la frase sugiere ("agregar esos campos") — candidata a partirse en tarea propia una vez se confirme alcance.
  - **T-003**/**T-004** (ID visual del socio en check-in / código numérico de auto-check-in): señalada la superposición obvia entre ambas — probablemente el mismo campo sirve para las dos, pero se registraron separadas tal como las mandó el usuario, con nota cruzada para no duplicar el concepto al implementar. T-004 también señala una pregunta de seguridad (rate limiting contra fuerza bruta de un código corto) que no estaba en el pedido original.
- Nada de esto está gateado todavía — no hay código nuevo, no hay migración generada. Arranca recién con `#run`/`#run-all`, y ahí primero hay que cerrar los open_questions de cada una antes de tocar código (mismo patrón que T-007/T-002/T-003 en la sesión anterior).

## Sesión 2026-08-25 (cont.) — `#run-all` de T-20260825-001 a -004
- Usuario mandó `#run-all`. Las 4 tareas tenían open_questions explícitas sin resolver — se cerraron todas vía `AskUserQuestion` en varias rondas (12 preguntas en total), registrando cada decisión en `tasks.md` antes de tocar código. Resultados no obvios (el usuario no siguió la opción recomendada en varios casos):
  - T-001: "quién está conectado" = **presence tracking en tiempo real** del equipo (no el widget simple de identidad+logout que se había sugerido como default) + restringir **también el acceso a datos** de `GET /api/v1/staff` a owner-only (no solo ocultar el link del nav) — ambas más grandes que la lectura recomendada.
  - T-002: **reemplazar** la invitación por email por credenciales que define el owner (confirmado a sabiendas del antipatrón de seguridad), `username` como campo separado del email, fichaje separado en tarea propia (nueva **T-20260825-005**), 2FA solo como restricción de diseño.
  - T-003/T-004: confirmado que son **dos campos distintos** (no el mismo, como se había sugerido para evitar duplicar el concepto) — `members.short_code` (formato 2 letras+2 números+2 letras) y `members.checkin_code` (6 dígitos) son identificadores separados.
  - A mitad de la ronda de preguntas, el usuario mandó un `#task` nuevo (Horarios: botones tabla/kanban/calendario) — registrado como **T-20260825-006**, con 5 open_questions propias, sin ejecutar.
- Antes de implementar, se preguntó al usuario cómo quería avanzar con T-001 a T-004 (ya listas para `#run`): eligió **"en paralelo vía subagentes"**.
- **Intento de aislamiento con git worktree (falló):** el `Agent` tool con `isolation: "worktree"` rechazó los 4 worktrees recién creados por un chequeo de seguridad (`core.worktree redirect`) que dio falso positivo en este entorno Windows — el `.git` redirect de cada worktree era válido (`gitdir: .../worktrees/agent-...`), no había `core.worktree` mal configurado en el repo principal. Sin causa raíz identificable en el tiempo disponible; se documenta acá para que una sesión futura (o el equipo de la herramienta) lo investigue si vuelve a pasar. Los 4 worktrees quedaron huérfanos, lockeados por el propio harness (no se forzó su remoción).
- **Pivot: ejecución secuencial sin aislamiento**, en el mismo working directory, en el orden que minimiza colisiones de archivo (T-003 → T-004 → T-001 → T-002, dado que T-003/T-004 comparten `members.ts`+`checkin-page` y T-001/T-002 comparten `staff/route.ts`). Cada subagente: implementa, genera migración local (`db:generate`, nunca `db:migrate`), corre `tsc`/`eslint`/`test:unit`, escribe su propio gate en `graph/gates/pending/`, comitea solo los archivos que tocó, y NO edita `tasks.md`/`progress.md` (reservado para esta sesión, evita conflictos entre los 4).
- **T-20260825-003** (commit `77064f4`): `members.short_code`, unique `(tenant_id, short_code)`, NOT NULL con backfill en la misma migración (`0010_known_mordo.sql`). Mostrado en check-in/lista de socios/ficha. Gate `medium`/async.
- **T-20260825-004** (commit `62a1aa1`): `members.checkin_code` (6 dígitos, unique por tenant, `0011_tiresome_scarlet_spider.sql`), endpoint `POST /api/v1/checkins/self` **dentro de la sesión autenticada existente** (decisión propia documentada: no se construyó un kiosco público sin login, que no estaba pedido explícitamente y es una superficie de riesgo mayor — señalado para revisión si la intención real era esa), rate limiting en memoria (fixed-window, 5 intentos/min, con caveat de que no sobrevive cold start en Vercel serverless). Nuevo valor de enum `"self_code"` en `checkinMethodEnum` (primer `ALTER TYPE ... ADD VALUE` del proyecto). Gate `medium`/async.
- **T-20260825-001** (commit `7ce6d9a`): presencia vía Supabase Realtime, canal `presence:tenant:{tenantId}`; logout reutiliza `supabase.auth.signOut()` existente; `GET /api/v1/staff` → owner-only; nav "Equipo" filtrado client-side (rol pasado como prop desde `layout.tsx`, que ya es Server Component). Gap señalado: `app/(owner)/layout.tsx` ya redirige a staff/member fuera de todo el grupo `(owner)` desde antes — el widget de presencia hoy solo puede mostrar al owner en la práctica; el cambio de `GET` es hardening defensivo, no cierre de un hueco explotable ahora mismo. Sin migración. Gate `medium`/async.
- **T-20260825-002** (commit `3fae426`): `POST /api/v1/staff` pasa de `admin.auth.admin.inviteUserByEmail` a `admin.auth.admin.createUser({email,password,email_confirm:true})`, mismo patrón de compensación (borra la cuenta Auth si falla el insert). `staff_members.username` nuevo, unique `(tenant_id, username)`, display-only (login sigue siendo email+password). Password mínimo 8, reusa la regla ya existente en `lib/validations/auth.ts`. Reset de password fuera de alcance (gap documentado). Migración `0012_melted_lethal_legion.sql` — **gate `high`, síncrono, requiere aprobador nombrado**, señala confirmar 0 filas reales en `staff_members` antes de aprobar (columna `NOT NULL`).
- Cada agente corrió `tsc`/`eslint` limpios. `test:unit` mostró fallas de conexión (`PostgresError 53300`, pool saturado) en varios archivos de integración desde T-001 en adelante — confirmado por cada agente como ambiental/no relacionado a su código (los archivos que fallan no importan ninguno de los módulos tocados); no se investigó la causa raíz del pool en esta sesión.
- Ninguna migración (`0010`, `0011`, `0012`) fue aplicada contra la base real — las tres quedan pendientes del mismo `npm run db:migrate`, gateado por la aprobación síncrona de T-002 (el `high` de la cadena).
- Fuera del backlog de tareas, en esta misma sesión: se instaló el skill `code-review-and-quality` (`.agents/skills/code-review-and-quality/SKILL.md`, pegado por el usuario — referencia dos archivos/skills que no existen en este repo, `security-and-hardening`/`performance-optimization` y sus checklists, señalado pero no creado) y se agregó la marketplace+plugin `superpowers@superpowers-dev` (`https://github.com/obra/superpowers`, scope usuario — aplica a todas las sesiones de Claude Code de esta persona, no solo este proyecto).

## Próxima sesión debería
- Revisar `circuit-breaker.yml`, `gates/policy.yml` y `roles/registry.yml` (siguen con los defaults del template, pendientes en el checklist de instalación). El `no_progress_threshold: 20` disparó dos falsos positivos en la sesión anterior — vale la pena que el usuario decida si lo ajusta.
- **Backlog en 0 tareas en curso, 12 completadas, grafo reindexado (190 nodos / 100 edges / 9 comunidades) al 2026-08-25** — no hay nada bloqueado ni gate sin aprobar en este momento.
- Verificación visual en navegador pendiente para varias features construidas sin sesión real disponible: picker+quick-add de actividades (T-008), sidebar (T-20260824-001), recordatorios en ficha de socio (T-20260824-003), pantalla Equipo + invitar (T-20260821-007/T-20260824-002).
- Config de deploy pendiente (no es código): `CRON_SECRET` en Vercel + dominio verificado en Resend para que los recordatorios de T-20260824-003 manden mail de verdad; configurar el envío de invitaciones de Supabase Auth (SMTP) para que T-20260821-007 pueda dar de alta gente real.
- Gaps de test coverage conocidos, no bloqueantes: `tests/integration/tenant-isolation/rls-policies.test.ts` no cubre `email_send_log` ni `staff_members` (sí cubre `activities`/`class_schedules`).
- Si el usuario retoma la idea de Linear: los otros 3 ítems sugeridos siguen sin pedirse (selector de negocio/tenant, empty states con ilustración+CTA, configuración agrupada en cards) — no asumir que están en scope hasta que haya un `#task` explícito.
- Seguir respetando el flujo `#task` (solo registrar) → gate si aplica → `#run` de acá en adelante.

## Notas adicionales
- El indexador built-in es regex-based, no AST completo — imports dinámicos o alias no relativos pueden no resolverse en `references`/`referenced_by`.
- `AGENTS.md` y `CLAUDE.md` en la raíz del repo estaban borrados en el working tree (sin commitear) al momento de instalar GRAPH; se recrearon desde el template del plugin porque el flujo de instalación los trata como "no encontrados". Si esa eliminación había sido intencional, revisar el contenido actual de ambos archivos.
