# Gate: T-20260826-001 — Convención de ubicación para helpers no-hook extraídos de un feature

- severity: medium (`edit_non_prod_file` en `gates/policy.yml` — la tarea se auto-flageaba `low`, pero policy.yml no deja severidad sin clasificar por criterio propio de la tarea; `edit_non_prod_file` es la clasificación explícita que corresponde)
- approval_mode: async (reorganización de archivos + cambio de rutas de import, sin cambio de comportamiento ni de schema)
- created: 2026-08-26

## Qué se hizo

Resuelta la `open_question` de la tarea vía `AskUserQuestion`: **agrupar en subcarpeta `lib/`** (no dejar sueltos en la raíz del feature).

- `features/dashboard-page/{format,calendar,sparkline}.ts` → `features/dashboard-page/lib/{format,calendar,sparkline}.ts` (`git mv`, contenido sin tocar).
- `features/dashboard-page/index.tsx` — 3 imports (`./format` → `./lib/format`, `./calendar` → `./lib/calendar`, `./sparkline` → `./lib/sparkline`).
- `features/dashboard-page/lib/calendar.ts` — su propio import interno (`from "./format"`) queda igual: ambos archivos se movieron juntos a `lib/`, siguen siendo hermanos.
- **Consistencia de convención (implícita en la open_question — "si se elige subcarpeta, esta tarea también debería mover `day-labels.ts`"):** `features/schedules-page/day-labels.ts` → `features/schedules-page/lib/day-labels.ts` (`git mv`), con sus 3 importadores actualizados (`schedule-table-view.tsx`, `schedule-kanban-view.tsx`, `schedule-form-dialog.tsx`, los 3 en `components/`, ruta `../day-labels` → `../lib/day-labels`).

Ningún archivo cambia de contenido salvo las líneas de import listadas arriba. Cero cambio de comportamiento.

## Por qué requiere revisión

- Fija una convención de proyecto (dónde viven los helpers puros no-hook, no-componente de un feature) que no existía documentada — futuras extracciones similares (el skill `react-architecture` va a seguir generando este caso) van a seguir este mismo patrón.
- Toca 8 archivos fuera del feature original de la tarea (los 3 importadores de `day-labels.ts` en `schedules-page`) para no dejar dos convenciones conviviendo — alcance ligeramente mayor al literal de la tarea, documentado por transparencia.

## Verificación

- `npx tsc --noEmit`: limpio, sin salida.
- `npx eslint features/dashboard-page features/schedules-page`: limpio, sin salida.
- `npm run test:unit`: 23/34 verdes, 11 fallas por `PostgresError 53300` (pool de conexiones agotado) en archivos de integración que no importan ningún módulo tocado por esta tarea — mismo patrón ambiental documentado repetidamente en sesiones anteriores (ver `progress.md`), no relacionado a este cambio.

## Gaps conocidos

- No se reindexó el grafo (`knowledge/`) tras el movimiento de archivos — los nodos `features__dashboard-page__{format,calendar,sparkline}.ts.json` y `features__schedules-page__day-labels.ts.json` quedan con paths desactualizados hasta el próximo `--reindex`.

## Aprobación

- [ ] Aprobado por:
- [ ] Fecha:
- Sin comando post-aprobación pendiente — no hay migración ni cambio de datos reales en esta tarea.
