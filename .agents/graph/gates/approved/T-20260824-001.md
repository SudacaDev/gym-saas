# Gate: T-20260824-001 — Sidebar fijo (app-shell) reemplaza nav horizontal

- severity: medium (`edit_non_prod_file` en `gates/policy.yml` — cambio de UI en `app/(owner)/**`, no toca base de datos ni config del propio patrón)
- approval_mode: async (no bloquea el hilo principal, queda registrado)
- created: 2026-08-24

## Qué se intenta

Reemplazar la barra de navegación horizontal (`app/(owner)/owner-nav.tsx` + `.module.css`) por un sidebar fijo vertical estilo app-shell de SaaS (icono + label por ítem, agrupados bajo un label de sección), ajustando `app/(owner)/layout.tsx` + `.module.css` de columna (header arriba, main abajo) a fila (sidebar a la izquierda, main a la derecha). Sin tocar paleta ni tipografía de `DESIGN.md` — mismos tokens de color, mismo rol Label (uppercase, tracked, bold) para los links.

## Por qué requiere revisión

Cambio estructural de layout en código no productivo, reversible, sin tocar datos ni servicios externos. Severidad `medium` según `default_severity_by_action.edit_non_prod_file` en `policy.yml` — confirmada explícitamente contra la policy antes de este gate, tal como quedó anotado en `tasks.md` como pendiente al crear la tarea.

## Opciones consideradas

- Sidebar colapsable con toggle/hamburguesa: descartado por ahora — agrega estado e interacción nueva no pedida; el usuario acordó arrancar solo por el sidebar fijo (ítem 1 de 4 sugeridos).
- Sidebar de ancho fijo en todos los breakpoints (incluido mobile): descartado — el check-in (dispositivo primario mobile/tablet según `DESIGN.md`) perdería demasiado ancho útil. Se implementa un rail icon-only por debajo de `md` (sin JS, solo CSS) en vez de ocultarlo del todo o forzar el ancho completo.
- Tocar las 4 sugerencias completas del análisis original (sidebar + selector de tenant + empty states + config en cards): fuera de alcance — el usuario solo pidió el sidebar en esta tarea.

## Estado

Aprobación async — procediendo a implementar. Queda registrado acá para revisión posterior del usuario, sin bloquear el hilo.
