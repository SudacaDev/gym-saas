# Progress

> Este archivo es la memoria persistente entre sesiones (principio P).
> Cada sesión de trabajo debe actualizar esto antes de cerrar — no confiar
> en que el contexto de la conversación sobreviva.

## Estado actual
- Instalación: brownfield — completada el 2026-08-20
- Indexación inicial: completa (55 nodos / 42 edges / 7 comunidades al instalar)
- Reindexación más reciente: 2026-08-25 (`/graph-ia:graph --reindex`) — 190 nodos / 100 edges / 9 comunidades. **Desactualizado** — todavía no incluye T-20260825-007.
- Reconciliación de historial (solo brownfield): importado vía git log — ver `graph/history/pre-graph-commits.md`
- Backlog activo: ver `tasks.md`. **T-007 a T-016 y T-20260827-001/002 completadas.** Pendiente: **T-20260827-003** (nombre de socio en `/checkin` → link a su ficha, para agregar membresía sin salir del flujo — navegación pura, la funcionalidad de membresía ya existe). T-014 quedó con alcance acotado a "solo recepción" (ventas/contabilidad/gerencia sin reglas definidas, documentado como gap para una tarea futura).
- **Bug real encontrado y corregido (2026-08-27), no ambiental:** pool de conexiones a Postgres agotado en dev — `db/client.ts` no sobrevivía a HMR (creaba un pool nuevo por recarga sin cerrar el anterior) sobre la conexión directa (`:5432`, no el pooler de Supabase). Explica retroactivamente varias fallas "ambientales" de `PostgresError`/pool documentadas en sesiones anteriores. Fix: cachear el pool en `globalThis`. Si reaparece el error, reiniciar el dev server (el fix no libera conexiones ya colgadas de antes del fix).
- **Migraciones `0014`-`0016` aplicadas y verificadas contra la base real (2026-08-26)** — `class_occurrences`/`class_reservations` (T-011), `products`/`walk_in_sales` (T-012), `leads` (T-013), `operational_requests` (T-010), las 6 con RLS activo. Aprobadas juntas vía `AskUserQuestion` porque `npm run db:migrate` no permite aplicar un subconjunto de migraciones pendientes.
- Última ejecución de `#run`: `#run T-20260826-009` el 2026-08-26 — pantalla nueva "Mi perfil" para staff (no `cleaning`): teléfono/certificaciones editables, cambio de password inmediato, cambio de email (con gap documentado: no hay callback para resincronizar `users.email` tras la confirmación de Supabase). Gate `medium`/async sin aprobar todavía.
- Última ejecución de `#run`: `#run T-20260826-005` el 2026-08-26 — skeleton de `plans-page` reemplaza "Cargando...". Gate `medium`/async sin aprobar todavía. **Atención:** `test:unit` dio 23/34 con 5 fallas de integración por error de conexión a la base (`TypeError` en cleanup, no el `PostgresError 53300` habitual) — tratado como ambiental sin investigar a fondo, revisar si se repite.
- **Circuit breaker:** se disparó dos veces durante T-20260821-004 (detalle completo más abajo), sin repetirse en ninguna sesión posterior, incluida esta.
- **Gates (actualizado 2026-08-26 tras el `#run-all` de T-010/011/012/013/014/015):** ningún gate síncrono pendiente — los 5 `high`/sync de esta sesión (T-010, T-011, T-012, T-013, T-014) quedaron aprobados. Queda `T-20260826-015` (`medium`/async) en `gates/pending/` esperando revisión, sin bloquear, junto con el resto de gates async acumulados de esta sesión (001 en adelante). **Observación:** los 16 gates de sesiones anteriores (`T-20260821-002-redo` a `T-20260825-009`) aparecen movidos a mano, fuera de esta sesión, a un nuevo `gates/approved/` sin commitear — ver detalle en la sesión 2026-08-26 más abajo. Las 3 migraciones (`0010` short_code, `0011` checkin_code, `0012` username de staff) siguen **aplicadas y verificadas contra la base real** — ver detalle del incidente de aplicación en la sesión del 2026-08-25 más abajo (backfill faltante en `0012`, corregido antes de aplicar).
- **Pendiente para que los recordatorios manden mail de verdad en producción (no es código, es config de deploy):** configurar `CRON_SECRET` en Vercel y verificar un dominio propio en Resend si todavía no está (mismo requisito que ya tenía el recibo de pago).

## Última sesión
- Fecha: 2026-08-21
- Qué se hizo:
  - Instalación de GRAPH en modo brownfield con `--migrate` (sin archivos previos que migrar).
  - Ajuste de contenedor en `app/(owner)/layout.tsx`: `max-w-4xl` → `max-w-[1580px]`.
  - Migración de clases Tailwind inline (`className="..."`) a archivos `*.module.css` colocados junto a cada componente bajo `app/**` (~19 archivos), usando `@apply` + `@reference "<ruta a globals.css>"`. Alcance acordado con el usuario: `components/ui/*` (primitivos shadcn con `cva`) quedaron fuera intencionalmente.
  - Extracción de los mapas de clases dinámicas (`STATUS_CLASSES`, `PAYMENT_STATUS_CLASSES` en `checkin/page.tsx` y `members/[memberId]/page.tsx`) para que referencien clases de CSS module (`styles.statusActive`, etc.) en vez de strings Tailwind crudos.
  - Reindexación del grafo tras la migración (`--reindex`): 122 nodos / 67 edges / 7 comunidades.
  - `/impeccable document`: generado `DESIGN.md` (+ sidecar `.impeccable/design.json`) capturando el sistema visual incumbente ("The Box Ledger" — dark-only, un acento naranja, flat-by-default, Archivo Black uppercase). No existía `DESIGN.md` previo.
  - Sección de planes (`features/plans-page`): tabla → grilla de cards (T-20260821-002 — ver nota de proceso, se ejecutó sin pasar por `#run` ni por el gate `medium` que le correspondía).
  - `#task` nuevo del usuario: horarios semanales. Registrado como dos tareas encadenadas (T-20260821-003 schema, T-20260821-004 UI, 004 depende de 003) — esta vez SÍ se registró y esperó, sin ejecutar nada hasta que el usuario mandó `#run-all`.
  - Antes de definir T-20260821-004, se convocó en paralelo a `marketing-director` y `uiux-designer` (a pedido explícito del usuario, que delegó esa decisión) para resolver "sección Horarios propia vs. panel en dashboard". Ambos coincidieron, independientemente, en sección propia — razonamiento completo en `tasks.md`.
  - `#run-all`: ejecutó T-20260821-003 hasta el límite de lo local — `db/schema/enums.ts` (+`dayOfWeekEnum`), `db/schema/class-schedules.ts` (tabla nueva, scope v1 estrictamente declarativo: día/hora/actividad, sin cupo/instructor/reservas por decisión de marketing), `db/schema/relations.ts`, `db/schema/index.ts`, `db/policies/0003_class_schedules_rls.sql` (mismo patrón que las 6 tablas existentes), migración generada con `npm run db:generate` (`db/migrations/0004_nappy_mattie_franklin.sql`, sin tocar la DB real), y extensión de `tests/integration/tenant-isolation/rls-policies.test.ts` para cubrir la tabla nueva. `tsc --noEmit` limpio en todo. Frenó ahí: aplicar la migración es `high` severity, así que se abrió gate síncrono en `graph/gates/pending/T-20260821-003.md` en vez de correr `npm run db:migrate`.
  - Aprobación síncrona del gate `high`: usuario ("SudacaDev") aprobó explícitamente correr la migración. `npm run db:migrate` ejecutado y verificado contra la base real (`class_schedules`: 8 columnas, `relrowsecurity`/`relforcerowsecurity` en `true`, policy `tenant_isolation` presente). T-20260821-003 pasa a completada.
  - Usuario preguntó por el estado de T-20260821-002 (visto en `tasks.md` abierto en el IDE) y pidió explícitamente revertirla y re-correrla por el flujo correcto. Se revirtió `features/plans-page/index.tsx`/`index.module.css` a la tabla original, se registró el gate async correspondiente (`graph/gates/pending/T-20260821-002-redo.md`, severidad `medium`), y se reimplementó la grilla de cards — mismo resultado, esta vez con rastro de proceso completo.
  - Nuevo `#task` del usuario mientras tanto: agregar a la tabla de socios fecha de cumpleaños, DNI y datos legales/de salud (certificado médico, problemas de salud). Registrado como **T-20260821-005**, sin ejecutar — incluye flag de dato sensible (Ley 25.326) como open_question a resolver antes de implementar.
  - Se retomó T-20260821-004 (UI de horarios): exploración de convenciones de rutas/API/formularios existentes (`app/(owner)/plans/page.tsx`, `app/api/v1/plans/**`, `lib/auth/require-role.ts`, `plan-form-dialog.tsx`) para replicar el mismo patrón en horarios.
  - **Primer corte del circuit breaker:** a mitad de la exploración de convenciones (varias lecturas seguidas sin editar ningún archivo), se disparó `stagnation` (`consecutive_no_progress: 20`, señal `git_diff_hash_unchanged`) — bloqueó Bash y Edit vía hook `PreToolUse`. Se reportó al usuario con el detalle completo (incluyendo que el propio `circuit-breaker.yml` documenta esta señal como propensa a falsos positivos en secuencias de lectura/verificación legítimas) y se esperó su revisión, sin intentar bypasear el marcador `.circuit-breaker-tripped`. El usuario respondió "podes seguir"; el marcador ya no existía en el siguiente intento y las herramientas volvieron a funcionar — no quedó claro por qué mecanismo exacto se limpió (no hay adaptador visible en el repo, `enforcement/claude-code-hook.sh` vive fuera de `.agents/`, en `${CLAUDE_PLUGIN_ROOT}`), documentado acá como comportamiento observado, no verificado en código.
  - Se construyó T-20260821-004 completa: `lib/validations/schedule.schema.ts`, `app/api/v1/schedules/route.ts` + `[id]/route.ts` (permisos `["owner","staff"]` en las 4 operaciones, a diferencia de `plans` que restringe escritura a `owner` — decisión explícita del usuario), `features/schedules-page/` (grid semanal 7 columnas hairline, patrón `benefits`-grid de `DESIGN.md`, colapsa en mobile), `app/(owner)/schedules/page.tsx`, link "Horarios" en el nav. `tsc`/`eslint` limpios.
  - Corrido el detector mecánico de `impeccable` (paso obligatorio del skill tras UI) sobre `schedules-page` y `plans-page`: encontró un advisory (`10px` fuera de la escala tipográfica en `plans-page`, no relacionado a T-004 pero corregido de paso a `11px`, el tamaño Label documentado). Segunda corrida: limpio.
  - **Segundo corte del circuit breaker:** al final, una racha de comandos de verificación (`tsc`, `eslint`, detector dos veces, `tsc` final) que no tocan el working tree entre sí volvió a disparar `stagnation`. Mismo protocolo: se reportó, no se bypaseó, se esperó revisión. El usuario abrió `circuit-breaker.yml` en el editor y respondió "podes seguir" de nuevo.
  - Mientras el segundo corte estaba activo, el usuario mandó dos `#task` nuevos. Se capturó su contenido en la respuesta al usuario (sin persistir, ya que Edit estaba bloqueado) y se registraron recién después de la segunda luz verde: **T-20260821-006** (ocultar "ingreso del mes" a staff) y **T-20260821-007** (alta real de staff con formulario especializado por categoría — profesores/administrativos/limpieza). Ninguna de las dos se ejecutó.
  - `#run T-20260821-005`: antes de tocar el schema se resolvieron las dos open_questions con el usuario (`AskUserQuestion`) — columnas directo en `members` (no tabla separada) y mismo acceso para owner/staff sobre el dato de salud (sin restricción adicional). Se construyó: `db/schema/members.ts` (+4 columnas: `birth_date`, `dni`, `medical_certificate_submitted`, `health_notes`), validación de DNI argentino (7-8 dígitos) en `lib/validations/member.schema.ts`, API `members` actualizada, campos nuevos en `member-form-dialog.tsx` (fecha, DNI, textarea de salud, checkbox de certificado), y la ficha de `member-detail-page` mostrando los 4 campos (certificado como `StatusPill`). `tsc`/`eslint`/detector de `impeccable` limpios. Migración generada (`db/migrations/0005_modern_nick_fury.sql`, puramente aditiva), gate `high` abierto en `graph/gates/pending/T-20260821-005.md`, aprobado por el usuario ("apruebo npm run db:migrate?"), `npm run db:migrate` corrido y verificado contra la base real (4 columnas confirmadas con tipo/nullability correctos vía script de verificación descartable).
  - Durante T-20260821-005 se notó (no se tocó en el momento) que `features/schedules-page/` cambió en disco fuera de esta sesión — el usuario evolucionó la grilla semanal a una matriz real día×hora (filas de horario, columnas de día, headers sticky). Quedó terminada y compilando por su cuenta (confirmado más tarde vía `tsc`).
  - El usuario agregó directo a `tasks.md` (fuera de mi flujo de escritura, visto vía system reminder de "archivo cambiado en disco") dos tareas más, con el mismo formato de registro: **T-20260821-008** (catálogo de actividades) y **T-20260821-009** (chips de día también en "Editar" horario). Tratadas como legítimas, sin revertir.
  - `#run-all`: recorrido en orden T-006 → T-007 → T-008 → T-009.
    - **T-006:** implementada con el default declarado (ocultar todo el bloque de ingresos — label/valor/tendencia — para `role !== "owner"`, dejar el resto del hero igual). La segunda open_question (qué mostrar en el lugar vacío) se resolvió por decisión propia: nada, sin placeholder. Cambio de una línea en `features/dashboard-page/index.tsx` (wrap condicional), sin tocar `metrics.ts` (Server Component, el dato no renderizado no llega al cliente igual). `tsc`/`eslint`/detector limpios (6 advisories de tamaño de fuente preexistentes en ese módulo, no relacionados, no tocados).
    - **T-007:** salteada — sigue bloqueada por falta de los campos por categoría, severidad `high`. Movida a "Tareas salteadas (con motivo)" en `tasks.md`, no se inventó nada.
    - **T-008:** antes de tocar el schema se resolvieron con `AskUserQuestion` las dos decisiones que cambiaban su forma — quick-add dentro del diálogo de horario (no página propia) y FK real `activity_id` (no solo autocompletar). El borrado de actividad en uso se decidió por precedente propio del código (mismo patrón `ON DELETE RESTRICT` + 409 que `plans`→`memberships`), sin pregunta nueva. Antes de escribir la migración se verificó contra la base real que `class_schedules` ya tenía 6 filas reales con `activity_name` ("Localizada", "Funcional" ×2, "PowerFit" ×3) — no era una tabla vacía, así que la migración necesitaba backfill de datos, no solo estructura. Se resolvió en dos pasos para evitar la heurística de detección de rename de `drizzle-kit` (que se dispara en modo interactivo si un mismo diff agrega y quita columnas parecidas): **pass A** (hecho, gate `high` abierto en `graph/gates/pending/T-20260821-008-pass-a.md`, sin aprobar todavía) agrega `activities` + `activity_id` nullable + backfillea las 3 actividades distintas y reasigna las 6 filas existentes, con SQL de backfill agregado a mano después de la parte generada por `drizzle-kit generate`; **pass B** (no empezado) hará `activity_id NOT NULL` + drop de `activity_name` + el rework de UI/API, en un gate `high` separado una vez que A esté aplicado y verificado.
    - **T-009:** unificado el picker de días de `schedule-form-dialog.tsx` — "Editar" pasó del `Select` de un solo día a los mismos chips multi-día que "Nuevo horario", pre-tildando el día original. `onSubmit` diffea contra ese día: se mantiene → `PATCH`; se destilda → `DELETE` sin `confirm()` extra (default confirmado por el usuario) vía el callback nuevo `onRemoved`; día nuevo tildado → `POST` con los mismos valores del formulario (segundo default confirmado). `onRemoved` wireado en `SchedulesPage`. Imports no usados limpiados. `tsc`/`eslint`/detector limpios.
- Tareas completadas:
  - [x] T-20260821-001 — Migrar clases Tailwind inline a CSS por componente bajo `app/**`
  - [x] T-20260821-002 — Planes: tabla → grilla de cards (redo con proceso completo tras el desvío de la sesión anterior)
  - [x] T-20260821-003 — Horarios: schema + RLS + migración aplicada y verificada contra la base real
  - [x] T-20260821-004 — Horarios: UI completa
  - [x] T-20260821-005 — Socios: cumpleaños/DNI/certificado médico/notas de salud, schema + UI + migración aplicada y verificada
  - [x] T-20260821-006 — Dashboard: ingreso del mes oculto para staff
  - [x] T-20260821-009 — Horarios: chips de día unificados entre Nuevo y Editar
- Tareas en curso:
  - [ ] T-20260821-008 — Catálogo de actividades: pass A hecho, frenado en gate `high` (`graph/gates/pending/T-20260821-008-pass-a.md`) esperando aprobación síncrona antes de `npm run db:migrate`. Pass B (UI + API + drop de `activity_name`) no empezado.
- Tareas salteadas:
  - [~] T-20260821-007 — Alta real de staff + formulario por categoría — sigue bloqueada por falta de los campos por categoría (profesores/administrativos/limpieza). Severidad `high` (crea cuentas reales de Supabase Auth).
- Tareas pendientes que siguen en `tasks.md`:
  - [ ] circuit-breaker.yml revisado y ajustado a este proyecto
  - [ ] policy.yml revisado
  - [ ] roles/registry.yml — qué roles se activan en este proyecto
- Por qué se cortó por circuit breaker (dos veces, ambas en T-20260821-004): ver sesión anterior — no volvió a dispararse en el resto de esta sesión (T-005 en adelante).
- **Desvío de proceso (corregido, sesión anterior):** T-20260821-002 se ejecutó fuera de proceso una vez y se corrigió con revert + redo formal. Desde entonces se respetó el flujo `#task` → registrar y esperar → gate si aplica → `#run`/`#run-all` en T-003 a T-009.

## Sesión 2026-08-24
- Usuario compartió 7 capturas de Linear pidiendo que gym-saas "se vea más a un SaaS, más profesional". Análisis propio: la brecha más grande es de app-shell (nav horizontal tipo landing) no de paleta/tipografía (`DESIGN.md` ya resuelto). Se sugirieron 4 cambios; el usuario acordó arrancar solo por el sidebar (T-20260824-001), los otros 3 quedaron anotados en `tasks.md` como sugeridos sin pedir.
- `#task` registrado y `#run T-20260824-001` recibido en el mismo turno. Severidad confirmada contra `gates/policy.yml` (`edit_non_prod_file` → `medium`, async) antes de tocar código — quedaba pendiente esa confirmación desde que se registró la tarea. Gate async abierto en `graph/gates/pending/T-20260824-001.md`.
- Implementado: `app/(owner)/owner-nav.tsx` + `.module.css` pasaron de nav horizontal a sidebar vertical fijo (`sticky top-0 h-screen`) con brand arriba, un grupo "Gestión" y 5 links icono+label (`lucide-react`, ya era dependencia — `LayoutDashboardIcon`, `UsersIcon`, `CreditCardIcon`, `CalendarDaysIcon`, `ScanLineIcon`). Estado activo: borde izquierdo 2px Ignition Orange + fill `--accent` (único lugar del sistema donde el nav lleva background, antes no lo tenía). Colapsa a rail icon-only (sin label, sin JS/toggle) por debajo de `md` para no romper el check-in en mobile/tablet, que `DESIGN.md` documenta como dispositivo primario.
- `app/(owner)/layout.tsx` + `.module.css`: de columna (`header` arriba + `main` abajo) a fila (sidebar + `main`, con `min-w-0` en `main` para no desbordar con contenido ancho tipo tablas).
- `DESIGN.md` sección "Navigation" actualizada para reflejar el patrón nuevo (antes describía el underline horizontal, ahora describe el sidebar + su estado activo) — se consideró parte de la tarea, no scope creep, porque dejar esa sección desactualizada rompe G (grounded) para cualquier agente que la consulte después.
- `tsc --noEmit` y `eslint` limpios. No se verificó visualmente en navegador (requiere sesión real contra el Supabase del proyecto).
- No se tocaron las páginas individuales bajo `app/(owner)/**` — ninguna asumía el nav horizontal de forma que rompiera con el cambio (grep de `sticky`/`z-`/`position: fixed` bajo `app/(owner)/**` no encontró nada en conflicto).
- Los otros 3 ítems sugeridos (selector de negocio/tenant, empty states con ilustración+CTA, configuración agrupada en cards) siguen sin pedirse — no se tocaron.

## Sesión 2026-08-24 (cont.) — `#run T-20260821-008` (pass B)
- Usuario mandó `#run T-20260821-008` para retomar el pass B pendiente (picker de actividades). Se construyó todo el código local, generó la migración con `drizzle-kit generate` (sin tocar la base real) y se frenó antes de aplicarla — severidad `high` por precedente directo (mismo patrón que T-003/T-005/pass-A de este mismo T-008).
- `db/schema/class-schedules.ts`: `activityId` → `NOT NULL`, columna `activityName` eliminada. Migración generada (`db/migrations/0007_many_silver_surfer.sql`) es exactamente `ALTER COLUMN activity_id SET NOT NULL` + `DROP COLUMN activity_name` — sin backfill propio porque pass A ya dejó las 6 filas reales con `activity_id` asignado.
- `app/api/v1/activities/route.ts` (nuevo): `GET` lista el catálogo; `POST` es get-or-create atómico vía `onConflictDoUpdate` sobre `(tenant_id, name)` — se descartó un select-then-insert en dos pasos porque `withTenantContext` corre todo en una transacción real: un insert fallido por unicidad la deja abortada y cualquier query siguiente en la misma transacción también falla.
- `lib/validations/activity.schema.ts` (nuevo) y `schedule.schema.ts` (`activityId` uuid reemplaza `activityName` texto libre). `app/api/v1/schedules/route.ts` + `[id]/route.ts` actualizados a juego.
- `schedule-form-dialog.tsx`: el `Input` de texto libre para actividad pasó a un `Select` (mismo patrón `Controller` que `plan-form-dialog.tsx`) poblado con el catálogo, más un ítem "+ Nueva actividad" que revela un `Input` inline y crea la actividad sin cerrar el diálogo (POST a `/api/v1/activities`, get-or-create — si el operador escribe un nombre que ya existe, se reusa en vez de fallar).
- `schedules-page/index.tsx`: ahora carga el catálogo junto con los horarios y resuelve el nombre a mostrar vía un `Map<id, name>` (antes venía embebido en cada fila).
- `tests/integration/tenant-isolation/rls-policies.test.ts`: el seed de `class_schedules` ya no manda `activityName` (columna eliminada del schema).
- `npx tsc --noEmit`, `npx eslint .` y `npm run test:unit` (28/28) limpios. Suite de integración (`test:integration`) no corrida — requiere `DATABASE_URL` real y, en este caso, la migración ya aplicada, que es justo lo que está pendiente de aprobación.
- Gate síncrono `graph/gates/pending/T-20260821-008-pass-b.md` **aprobado** por el usuario vía `AskUserQuestion` ("Sí, correr la migración"). Primer `npm run db:migrate` falló antes de ejecutar ningún DDL propio: `PostgresError 53300` (pool de conexiones saturado). Base sin tocar — no se reintentó de inmediato para no seguir presionando el pool.
- El usuario pidió reintentar ("correlo vos al migrate") mientras, en paralelo, también lo corría en su propia terminal — se le avisó de no correrlo desde los dos lados a la vez. Segundo intento desde acá: la conexión ya no falló, pero apareció un error distinto y real: `PostgresError 23502` ("column activity_id ... contains null values"). El CLI de `drizzle-kit` seguía sin mostrar el error de fondo (spinner lo tapaba), así que se bypaseó corriendo `drizzle-orm/postgres-js/migrator` directo.
- Un `SELECT` de solo lectura confirmó la causa: **4 filas reales** de `class_schedules` (`activity_name = 'indoor cycling'`, creadas 2026-08-22T16:29:58Z) tenían `activity_id NULL` — se habían cargado, después de la verificación de pass A, con la API vieja que todavía no tocaba `activity_id`. No era un dato inventado por ningún gate anterior; el estado de la base simplemente avanzó porque la app se siguió usando entre pass A y pass B.
- Reportado al usuario con el detalle exacto (4 filas, fecha, causa) y una propuesta de backfill puntual (misma lógica que el backfill de pass A, acotada a esas 4 filas con `WHERE activity_id IS NULL`). Aprobado vía `AskUserQuestion` ("Sí, backfill + migrar").
- **Backfill ejecutado** en una transacción: 1 fila nueva en `activities` ("indoor cycling"), 4 filas de `class_schedules` actualizadas. Verificado: 0 filas `activity_id NULL` restantes.
- **Migración aplicada** (`activity_id NOT NULL` + drop de `activity_name`) + `apply-policies.ts` (4 archivos, sin cambios, idempotente).
- **Verificado contra la base real:** `class_schedules` con 8 columnas (`activity_name` ya no existe, `activity_id NOT NULL`), las 10 filas reales joinean correctamente contra `activities`, RLS forzada en ambas tablas. T-20260821-008 (pass A + pass B) queda **completa**.
- Los tres scripts de diagnóstico/backfill (`scripts/_diag-*.ts`) fueron disposables — creados y borrados dentro de esta sesión, nunca commiteados. Detalle completo del incidente y su resolución en `graph/gates/pending/T-20260821-008-pass-b.md`.

## Sesión 2026-08-24 (cont.) — `#run-all`
- Usuario mandó `#run-all`. Tres tareas pendientes en `tasks.md` (T-20260824-002, T-20260821-007, T-20260824-003) tenían preguntas abiertas explícitamente marcadas "no asumir, preguntar antes de `#run`" — se resolvieron las 4 relevantes vía `AskUserQuestion` antes de tocar código: T-002 = "Schema primero" (no mock), T-007 = confirmado el set de campos tal cual, y dos preguntas de Gmail (vencimiento de membership / Calendar afuera de v1) que quedaron sin usar porque, al releer el estado en disco, T-20260824-003 ya estaba completa — construida por otra sesión/ventana en paralelo mientras se hacían las preguntas (pivotó de OAuth de Gmail a reusar Resend, ya integrado en el proyecto; decisión sensata tomada con el usuario en esa otra sesión). No se rehizo nada de Gmail, se tomó el estado en disco como autoritativo.
- Se avisó al usuario del hallazgo (T-003 ya completa) y se pasó a construir T-20260821-007 + T-20260824-002 juntas, dado que la segunda quedó explícitamente dependiente de la primera por la respuesta "Schema primero".
- **T-20260821-007 (schema + alta real):** `db/schema/enums.ts` (+3 enums de staff), `db/schema/staff-members.ts` (nuevo — soft delete vía `deletedAt`, ajustado a la convención real de `members.ts` en vez del boolean `active` que sugería el plan original), `relations.ts`/`index.ts`, `db/policies/0006_staff_members_rls.sql`, migración generada `db/migrations/0009_cheerful_emma_frost.sql` (puramente aditiva, sin backfill). `lib/validations/staff-member.schema.ts` (alta con `superRefine` para exigir especialidades solo si `staffCategory === "instructor"`; edición sin `email`). `app/api/v1/staff/route.ts` (GET owner+staff; POST owner-only — `admin.auth.admin.inviteUserByEmail` + inserción transaccional de `users`+`staff_members`, con compensación explícita: si la transacción falla, se borra la cuenta de Auth recién creada para no dejarla huérfana) y `[id]/route.ts` (PATCH/DELETE owner-only, DELETE es soft delete).
- **T-20260824-002 (pantalla Equipo):** `features/staff-page/` completo (tabla + buscador cliente + `StaffFormDialog` con campos condicionales por categoría, reusando el patrón de chips de `schedule-form-dialog.tsx` para especialidades), `app/(owner)/staff/page.tsx`, link "Equipo" en `owner-nav.tsx` (`IdCardIcon`). "Última conexión" de la referencia de Linear quedó explícitamente afuera — vive en `auth.users.last_sign_in_at`, no modelado en Drizzle; traerla por fila implicaría N llamadas a la Admin API por request.
- `npx tsc --noEmit`, `npx eslint .` y `npm run test:unit` (34/34) limpios.
- Gate `high` abierto en `graph/gates/pending/T-20260821-007.md` (cubre ambas tareas, comparten la misma migración) — **sin aprobar todavía**, `npm run db:migrate` no corrido.
- Gaps conocidos documentados en el gate: sin extender el test de aislamiento RLS para `staff_members` (mismo gap que quedó en `email_send_log` de T-003), sin revocación real de acceso de Auth en el soft-delete, sin verificación visual en navegador.
- Usuario aprobó el gate (`AskUserQuestion`, "Sí, correr la migración"). `npm run db:migrate` corrido **sin incidentes** (a diferencia de T-20260821-008: sin problemas de conexión, sin datos huérfanos). Verificado contra la base real: `staff_members` con 17 columnas, tipos/nullability correctos, RLS forzada, policy `tenant_isolation` presente. T-20260821-007 y T-20260824-002 pasan a completadas.
- Fin de la racha de `#run-all` de esta sesión: backlog activo queda en 0 tareas en curso, 10 completadas hoy, el grafo de conocimiento sigue pendiente de reindexar (ver "Estado actual"). Ninguna cuenta real de staff fue creada — solo ocurre cuando alguien use "Invitar" en la app en producción.

## Sesión 2026-08-25 — `/graph-ia:graph --reindex`
- Usuario corrió `/graph-ia:graph --reindex` (sin `--mode`, refresh standalone). Ejecutado `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/build-graph.py <repo_root>` (el indexador propio del plugin, regex-based, sin dependencias de terceros) tal como indica `commands/graph-ia.md` Paso 7.
- Resultado: 190 nodos / 100 edges / 9 comunidades (antes: 122/67/7 al 2026-08-21) — `knowledge/index.json` sobreescrito con estos conteos reales, `last_indexed: 2026-08-25T14:45:25Z`.
- No se re-corrió la reconciliación de historial de git: `.agents/graph/history/pre-graph-commits.md` ya existía (marcador de que ya se había hecho), y el paso 7 explícitamente skipea ese re-run cuando ese archivo está presente — `--reindex` solo fuerza el índice de código, no el historial.
- Con esto, el grafo queda al día con todo lo construido en la sesión anterior (T-20260821-004 a -009, T-20260824-001/-002/-003) — ya no hay deuda de reindexación pendiente.

## Sesión 2026-08-25 (cont.) — 4×`#task`
- Usuario mandó 4 `#task` en un mismo mensaje. Registrados en `tasks.md` (T-20260825-001 a -004), **sin implementar nada** — flujo `#task` = solo registrar y esperar `#run`.
- Cada una quedó con sus open_questions explícitas, sin adivinar ninguna:
  - **T-001** (header de sesión + nav Equipo owner-only + ingreso mensual): "ingreso mensual solo owner" ya está hecho desde T-20260821-006 — señalado como posible duplicado/recordatorio, no reimplementado a ciegas. "Quién está conectado" se interpretó tentativamente como identidad+logout del usuario actual (no presence tracking del equipo), pero queda como pregunta a confirmar. Restringir "Equipo" a owner-only tensiona con el permiso `["owner","staff"]` en `GET /api/v1/staff` que se definió en T-007/T-002 — señalado, no resuelto.
  - **T-002** (usuario/contraseña + horario de entrada/salida + 2FA a futuro): marcada como tensión directa con el diseño de T-20260821-007 (invite-by-email, el owner nunca ve la contraseña) — el pedido de que el owner cargue usuario+contraseña directamente es un cambio de arquitectura de auth, no un agregado incremental. "Horario de entrada/salida" se señaló como una feature de asistencia/fichado bastante más grande de lo que la frase sugiere ("agregar esos campos") — candidata a partirse en tarea propia una vez se confirme alcance.
  - **T-003**/**T-004** (ID visual del socio en check-in / código numérico de auto-check-in): señalada la superposición obvia entre ambas — probablemente el mismo campo sirve para las dos, pero se registraron separadas tal como las mandó el usuario, con nota cruzada para no duplicar el concepto al implementar. T-004 también señala una pregunta de seguridad (rate limiting contra fuerza bruta de un código corto) que no estaba en el pedido original.
- Nada de esto está gateado todavía — no hay código nuevo, no hay migración generada. Arranca recién con `#run`/`#run-all`, y ahí primero hay que cerrar los open_questions de cada una antes de tocar código (mismo patrón que T-007/T-002/T-003 en la sesión anterior).

## Sesión 2026-08-25 (cont.) — `#run-all` de T-20260825-001 a -004
- Usuario mandó `#run-all`. Las 4 tareas tenían open_questions explícitas sin resolver — se cerraron todas vía `AskUserQuestion` en varias rondas (12 preguntas en total), registrando cada decisión en `tasks.md` antes de tocar código. Resultados no obvios (el usuario no siguió la opción recomendada en varios casos):
  - T-001: "quién está conectado" = **presence tracking en tiempo real** del equipo (no el widget simple de identidad+logout que se había sugerido como default) + restringir **también el acceso a datos** de `GET /api/v1/staff` a owner-only (no solo ocultar el link del nav) — ambas más grandes que la lectura recomendada.
  - T-002: **reemplazar** la invitación por email por credenciales que define el owner (confirmado a sabiendas del antipatrón de seguridad), `username` como campo separado del email, fichaje separado en tarea propia (nueva **T-20260825-005**), 2FA solo como restricción de diseño.
  - T-003/T-004: confirmado que son **dos campos distintos** (no el mismo, como se había sugerido para evitar duplicar el concepto) — `members.short_code` (formato 2 letras+2 números+2 letras) y `members.checkin_code` (6 dígitos) son identificadores separados.
  - A mitad de la ronda de preguntas, el usuario mandó un `#task` nuevo (Horarios: botones tabla/kanban/calendario) — registrado como **T-20260825-006**, con 5 open_questions propias, sin ejecutar.
- Antes de implementar, se preguntó al usuario cómo quería avanzar con T-001 a T-004 (ya listas para `#run`): eligió **"en paralelo vía subagentes"**.
- **Intento de aislamiento con git worktree (falló):** el `Agent` tool con `isolation: "worktree"` rechazó los 4 worktrees recién creados por un chequeo de seguridad (`core.worktree redirect`) que dio falso positivo en este entorno Windows — el `.git` redirect de cada worktree era válido (`gitdir: .../worktrees/agent-...`), no había `core.worktree` mal configurado en el repo principal. Sin causa raíz identificable en el tiempo disponible; se documenta acá para que una sesión futura (o el equipo de la herramienta) lo investigue si vuelve a pasar. Los 4 worktrees quedaron huérfanos, lockeados por el propio harness (no se forzó su remoción).
- **Pivot: ejecución secuencial sin aislamiento**, en el mismo working directory, en el orden que minimiza colisiones de archivo (T-003 → T-004 → T-001 → T-002, dado que T-003/T-004 comparten `members.ts`+`checkin-page` y T-001/T-002 comparten `staff/route.ts`). Cada subagente: implementa, genera migración local (`db:generate`, nunca `db:migrate`), corre `tsc`/`eslint`/`test:unit`, escribe su propio gate en `graph/gates/pending/`, comitea solo los archivos que tocó, y NO edita `tasks.md`/`progress.md` (reservado para esta sesión, evita conflictos entre los 4).
- **T-20260825-003** (commit `77064f4`): `members.short_code`, unique `(tenant_id, short_code)`, NOT NULL con backfill en la misma migración (`0010_known_mordo.sql`). Mostrado en check-in/lista de socios/ficha. Gate `medium`/async.
- **T-20260825-004** (commit `62a1aa1`): `members.checkin_code` (6 dígitos, unique por tenant, `0011_tiresome_scarlet_spider.sql`), endpoint `POST /api/v1/checkins/self` **dentro de la sesión autenticada existente** (decisión propia documentada: no se construyó un kiosco público sin login, que no estaba pedido explícitamente y es una superficie de riesgo mayor — señalado para revisión si la intención real era esa), rate limiting en memoria (fixed-window, 5 intentos/min, con caveat de que no sobrevive cold start en Vercel serverless). Nuevo valor de enum `"self_code"` en `checkinMethodEnum` (primer `ALTER TYPE ... ADD VALUE` del proyecto). Gate `medium`/async.
- **T-20260825-001** (commit `7ce6d9a`): presencia vía Supabase Realtime, canal `presence:tenant:{tenantId}`; logout reutiliza `supabase.auth.signOut()` existente; `GET /api/v1/staff` → owner-only; nav "Equipo" filtrado client-side (rol pasado como prop desde `layout.tsx`, que ya es Server Component). Gap señalado: `app/(owner)/layout.tsx` ya redirige a staff/member fuera de todo el grupo `(owner)` desde antes — el widget de presencia hoy solo puede mostrar al owner en la práctica; el cambio de `GET` es hardening defensivo, no cierre de un hueco explotable ahora mismo. Sin migración. Gate `medium`/async.
- **T-20260825-002** (commit `3fae426`): `POST /api/v1/staff` pasa de `admin.auth.admin.inviteUserByEmail` a `admin.auth.admin.createUser({email,password,email_confirm:true})`, mismo patrón de compensación (borra la cuenta Auth si falla el insert). `staff_members.username` nuevo, unique `(tenant_id, username)`, display-only (login sigue siendo email+password). Password mínimo 8, reusa la regla ya existente en `lib/validations/auth.ts`. Reset de password fuera de alcance (gap documentado). Migración `0012_melted_lethal_legion.sql` — **gate `high`, síncrono, requiere aprobador nombrado**, señala confirmar 0 filas reales en `staff_members` antes de aprobar (columna `NOT NULL`).
- Cada agente corrió `tsc`/`eslint` limpios. `test:unit` mostró fallas de conexión (`PostgresError 53300`, pool saturado) en varios archivos de integración desde T-001 en adelante — confirmado por cada agente como ambiental/no relacionado a su código (los archivos que fallan no importan ninguno de los módulos tocados); no se investigó la causa raíz del pool en esta sesión.
- Ninguna migración (`0010`, `0011`, `0012`) fue aplicada contra la base real — las tres quedan pendientes del mismo `npm run db:migrate`, gateado por la aprobación síncrona de T-002 (el `high` de la cadena).
- Fuera del backlog de tareas, en esta misma sesión: se instaló el skill `code-review-and-quality` (`.agents/skills/code-review-and-quality/SKILL.md`, pegado por el usuario — referencia dos archivos/skills que no existen en este repo, `security-and-hardening`/`performance-optimization` y sus checklists, señalado pero no creado) y se agregó la marketplace+plugin `superpowers@superpowers-dev` (`https://github.com/obra/superpowers`, scope usuario — aplica a todas las sesiones de Claude Code de esta persona, no solo este proyecto).

## Sesión 2026-08-25 (cont.) — aplicación de migraciones 0010-0012 + T-20260825-006
- Usuario aprobó explícitamente (`AskUserQuestion`) correr `npm run db:migrate` para las 3 migraciones pendientes, incluyendo el gate `high` de T-002.
- **Incidente durante la aplicación:** el primer intento (corrido por mí, en esta sesión) se colgó indefinidamente en el spinner "applying migrations..." — diagnosticado como un advisory lock de Postgres tomado por un proceso `npm run db:migrate` que el usuario había dejado corriendo/colgado en su propia terminal desde antes. El usuario confirmó que seguía abierto; se le pidió cerrarlo. Un segundo intento (del usuario, en su terminal) devolvió el prompt sin error visible pero **tampoco aplicó nada** — mi script de verificación inicial (contra `DATABASE_URL`, el rol restringido `app_user`) no mostraba las columnas nuevas. Se encontró la causa real: `drizzle.config.ts` usa `DATABASE_TRUSTED_URL ?? DATABASE_URL` para migraciones (rol elevado, necesario para DDL) — mi script de verificación no replicaba esa precedencia. Corregido, y con la conexión correcta se confirmó que las migraciones seguían sin aplicarse (última aplicada real: `0009`).
- **Causa raíz real:** entre la aprobación del gate de T-002 y su ejecución, apareció **1 fila real** en `staff_members` (alguien completó un alta de staff mientras tanto) — la migración `0012` hacía `ADD COLUMN username text NOT NULL` sin backfill, así que fallaba (violación de `NOT NULL`) contra esa fila, silenciosamente desde la perspectiva del usuario en su terminal. Se corrigió `db/migrations/0012_melted_lethal_legion.sql`: columna nullable → bloque `DO $$` que backfillea `username` desde el local-part del email de cada fila existente (normalizado, sufijo numérico si colisiona por tenant) → recién ahí `SET NOT NULL` + unique index. Mismo patrón que el backfill de `short_code`/`checkin_code`. No se consultó el email/nombre real de la persona directamente — el clasificador de auto mode bloqueó dos intentos de Bash que hubieran tocado PII real (una consulta SQL con email/nombre), así que el backfill se resolvió enteramente del lado del SQL sin que nadie viera el dato.
- Tercer intento (del usuario, con la migración corregida): **exitoso**. Verificado contra la base real (rol trusted): `members.short_code`/`checkin_code` (`NOT NULL`), `staff_members.username` (`NOT NULL`, la fila existente backfillada sin error), `checkin_method` con `self_code`. Los 3 gates (`T-20260825-002/-003/-004`) cerrados con su resultado. Scripts de verificación descartables (`scripts/_verify-2026-08-25.ts`, `scripts/_check-staff-row.ts`) borrados al terminar, nunca commiteados.
- **T-20260825-006** (vistas Horarios): 5 open_questions resueltas vía `AskUserQuestion` (todas con la opción recomendada) — "variables" era typo por "vistas"; la grilla existente se reusa como vista "tabla"; kanban por día de semana; calendario como grilla mensual sin modelo de excepciones (el schema no las soporta); default "tabla" por decisión propia. Implementado por `frontend-developer`: hook compartido `use-schedules.ts` extraído para no duplicar fetch/CRUD entre las 3 vistas, `schedule-table-view.tsx` (grilla existente extraída tal cual), `schedule-kanban-view.tsx` y `schedule-calendar-view.tsx` (nuevas). `tsc`/`eslint`/`test:unit` (34/34) limpios. Gate `low/medium`/async en `graph/gates/pending/T-20260825-006.md`. Commit `502949e`.
- Mientras tanto, el usuario agregó directo a `tasks.md` (fuera de mi flujo de escritura) una tarea nueva: **T-20260825-007** — en la vista Tabla de Horarios, las celdas vacías de la grilla día×hora deben ser clickeables y abrir "Nuevo horario" con día/hora precargados. Viene con contexto técnico detallado (requiere un prop nuevo `initialValues` en `ScheduleFormDialog`) y una open_question propia (default de `endTime` al precargar: propuesta +1 hora). Tratada como legítima, no revertida, no implementada todavía.

## Próxima sesión debería
- Revisar `circuit-breaker.yml`, `gates/policy.yml` y `roles/registry.yml` (siguen con los defaults del template, pendientes en el checklist de instalación). El `no_progress_threshold: 20` disparó dos falsos positivos en la sesión anterior — vale la pena que el usuario decida si lo ajusta.
- **Backlog en 0 tareas en curso, 12 completadas, grafo reindexado (190 nodos / 100 edges / 9 comunidades) al 2026-08-25** — no hay nada bloqueado ni gate sin aprobar en este momento.
- Verificación visual en navegador pendiente para varias features construidas sin sesión real disponible: picker+quick-add de actividades (T-008), sidebar (T-20260824-001), recordatorios en ficha de socio (T-20260824-003), pantalla Equipo + invitar (T-20260821-007/T-20260824-002).
- Config de deploy pendiente (no es código): `CRON_SECRET` en Vercel + dominio verificado en Resend para que los recordatorios de T-20260824-003 manden mail de verdad; configurar el envío de invitaciones de Supabase Auth (SMTP) para que T-20260821-007 pueda dar de alta gente real.
- Gaps de test coverage conocidos, no bloqueantes: `tests/integration/tenant-isolation/rls-policies.test.ts` no cubre `email_send_log` ni `staff_members` (sí cubre `activities`/`class_schedules`).
- Si el usuario retoma la idea de Linear: los otros 3 ítems sugeridos siguen sin pedirse (selector de negocio/tenant, empty states con ilustración+CTA, configuración agrupada en cards) — no asumir que están en scope hasta que haya un `#task` explícito.
- Seguir respetando el flujo `#task` (solo registrar) → gate si aplica → `#run` de acá en adelante.

## Sesión 2026-08-26 — `#run T-20260826-001`
- Usuario pidió leer `AGENTS.md` bien y correr `#run T-20260826-001`. Releídos `GRAPH.md`, `tasks.md`, `progress.md`, `roles/registry.yml`, `gates/policy.yml`, `circuit-breaker.yml` antes de tocar nada. Sin circuit breaker activo, sin gates síncronos pendientes, T-20260826-001 sin `depends_on`.
- La tarea traía una `open_question` marcada explícitamente "no asumir, resolver antes de `#run`" (ubicación de los helpers no-hook extraídos de `dashboard-page`). Resuelta vía `AskUserQuestion`: **agrupar en subcarpeta `lib/`** (no dejarlos sueltos en la raíz del feature).
- Ejecutado: `features/dashboard-page/{format,calendar,sparkline}.ts` → `features/dashboard-page/lib/` (`git mv`), 3 imports actualizados en `index.tsx`. Por consistencia (la propia open_question lo señalaba como consecuencia de elegir subcarpeta), también `features/schedules-page/day-labels.ts` → `features/schedules-page/lib/day-labels.ts`, con sus 3 importadores en `components/` actualizados — así no quedan dos convenciones conviviendo en el proyecto. Cero cambio de contenido más allá de las rutas de import.
- `tsc --noEmit` y `eslint` limpios. `test:unit`: 23/34 verdes, 11 fallas por `PostgresError 53300` (pool de conexiones agotado) en archivos de integración que no importan ningún módulo tocado — mismo patrón ambiental de sesiones anteriores, no investigado de nuevo.
- Gate `medium`/async abierto en `graph/gates/pending/T-20260826-001.md` (severidad real según `edit_non_prod_file` de `policy.yml` — la tarea se auto-flageaba `low`, pero la clasificación de policy.yml prevalece).
- **Observación fuera de esta tarea, no tocada:** al hacer `git status` se encontró que los 16 archivos que vivían en `gates/pending/` (de T-20260821-002-redo a T-20260825-009) ya no están ahí — aparecen como `D` (borrados, sin commitear) en `pending/` y existen físicamente, sin trackear todavía, en un nuevo directorio `gates/approved/` con los mismos nombres y timestamps de modificación entre 2026-08-21 y 2026-08-26. Esto no lo hizo esta sesión (no se tocó ningún archivo de `gates/` fuera de crear el propio `T-20260826-001.md` en `pending/`). Es consistente con el invariante de `roles/registry.yml` ("gates/approved/ solo la escribe el mecanismo de aprobación humana en sí, nunca un agente") — lectura más probable es que el usuario movió los gates ya aprobados a mano fuera de esta sesión. Señalado para que quede rastro; no se revirtió ni se commiteó nada de `gates/` salvo el archivo nuevo de esta tarea. `AGENTS.md`, `app/page.tsx` y `.circuit-breaker-state.json` también ya estaban modificados sin commitear al empezar esta sesión (no relacionado a T-20260826-001, no tocado).

## Sesión 2026-08-26 (cont.) — convención de hooks `hooks/use<Nombre>.ts`
- Usuario abrió `features/plans-page/index.tsx` en el IDE, señaló que ya había dejado `features/plans-page/hooks/usePlans.ts` como ejemplo explícito del patrón que quiere para el proyecto (hook cliente en `hooks/`, camelCase, dueño de estado/fetch/handlers) y pidió aplicarlo. Se limpiaron primero los imports de React (`useCallback`/`useEffect`/`useState`) que habían quedado sin usar en `index.tsx` tras esa extracción manual.
- Detectado el conflicto: `features/schedules-page/use-schedules.ts` ya hacía lo mismo pero en la raíz del feature, kebab-case — dos convenciones conviviendo. Se preguntó alcance vía `AskUserQuestion`: usuario eligió **migrar todo el proyecto ahora** (no solo documentar), incluyendo las 4 features que todavía manejaban estado inline en su `index.tsx` (`staff-page`, `checkin-page`, `members-page`, `member-detail-page`).
- Mientras se armaban las preguntas de alcance, el usuario interrumpió con "¿por qué usás grep si tenés reducto instalado?" — cierto: `AGENTS.md` paso 0 pide leer `reducto/*` y correr `reducto context` antes de grepear, no se había hecho. Confirmado que `reducto` es un CLI instalado (`reducto query`, indexa este repo real — probado contra `PlansPage`/`PlanFormDialog`, resultados correctos) — adoptado desde este punto para búsquedas de código, instruido también a los 5 subagentes de esta tarea.
- Tarea registrada como **T-20260826-002**, ejecutada vía **5 subagentes `frontend-developer` en paralelo** (primera vez que la paralelización funciona de punta a punta sin fallback a secuencial — a diferencia de la sesión 2026-08-25 donde el aislamiento por git worktree falló; acá los 5 conjuntos de archivos eran completamente disjuntos, sin necesidad de aislamiento):
  - `schedules-page/use-schedules.ts` → `schedules-page/hooks/useSchedules.ts` (mover + renombrar, 1 import actualizado).
  - `staff-page/hooks/useStaff.ts`, `checkin-page/hooks/useCheckin.ts`, `members-page/hooks/useMembers.ts`, `member-detail-page/hooks/useMemberDetail.ts` — extracción nueva en cada caso, mismo shape que `usePlans.ts`. `member-detail-page` (el más grande, ~20 hooks originalmente) quedó como un único hook por decisión del subagente (justificada: `currentMembership` es dependencia compartida entre el efecto de pagos y `handleSendReminder`, partir hubiera exigido prop-drilling entre archivos).
- Verificación de reglas de React vía skill `react-architecture` (paso obligatorio de `AGENTS.md` #11, por tratarse explícitamente de hooks): se leyeron los 6 hooks (5 nuevos/movidos + `usePlans.ts` de referencia) — todos llaman sus hooks incondicionalmente en el nivel superior, arrays de dependencias correctos en los 6. **Sin violaciones de reglas de hooks.** Discrepancia no bloqueante encontrada: la tabla de convenciones del propio skill documenta `use-<name>.ts` kebab-case, distinto del camelCase que el usuario confirmó — tratada como decisión de proyecto que prevalece sobre el default genérico del skill, no como error de React (no aplica la cláusula de "no corras la tarea" de `AGENTS.md` #11, que es para errores reales, no para preferencias de naming ya decididas).
- `npx tsc --noEmit` y `npx eslint .` (repo completo, tras integrar los 5 resultados): limpios. `npm run test:unit`: **34/34 verde** (9/9 archivos) — el `PostgresError 53300` que venía documentándose como ambiental en sesiones anteriores no se reprodujo esta vez.
- Gate `medium`/async abierto en `graph/gates/pending/T-20260826-002.md`, sin aprobar todavía. Backlog vuelve a 0 pendientes / 0 en curso.

## Sesión 2026-08-26 (cont.) — `#task` + `#run T-20260826-003`: Reducto solo resuelve el subgrafo de la tarea
- Usuario abrió `reducto-out/session_stats.json` y preguntó por qué `reducto context` mostraba `Tokens saved: 0`. Diagnóstico: `reducto context` mostró `2473/2473` nodos `Unknown`, `0` `Partial`/`Known` — las 5 queries de la sesión nunca usaron `--resolve`, así que no había línea base de comparación (`Tokens used` == `Without Reducto`, ambos 3.309). Confirmado con `reducto skill`: el flujo esperado es `resolve=false` para explorar barato y `resolve=true` recién cuando hace falta el código real.
- Usuario preguntó si eso se podía integrar al flujo de `#task`/`#run` para que cada tarea resuelva solo su propia porción del grafo, no el grafo entero. Registrada como **T-20260826-003** y ejecutada en el mismo turno (`#task` + `#run` explícitos del usuario).
- Ejecutado: `AGENTS.md` paso 8 (`#run`) gana una frase — antes de tocar código, resolver el subgrafo relevante vía `reducto query --resolve --view skeleton`, usando `path:` de la tarea si ya existe, o keywords del título si no. Explícitamente se prohíbe resolver el grafo entero o fuera del alcance de la tarea. No se renumeraron los pasos 9/10/11 (referenciados como "`AGENTS.md` #11" en el gate de T-20260826-002) para no romper esa referencia.
- Clasificado `medium`/`edit_non_prod_file` (no `high`/`edit_graph_config`, reservado para `circuit-breaker.yml`/`policy.yml`) — primer precedente de editar `AGENTS.md` mismo vía este flujo, señalado en el gate.
- Gate `medium`/async abierto en `graph/gates/pending/T-20260826-003.md`, sin aprobar todavía. **Gap señalado en el propio gate:** no se verificó si `reducto query --resolve` acepta paths de forma nativa o si hay que pasarlos como texto libre (mismo mecanismo que las keywords) — falta confirmar contra el `--help` real o probarlo en la próxima tarea. Tampoco se re-corrió `reducto context` después del cambio (no hay ninguna tarea nueva todavía que lo ejercite).

## Sesión 2026-08-26 (cont.) — `#task` + `#run T-20260826-004`: skeleton en `member-detail-page`
- Usuario pidió (con `owner-nav.module.css` abierto en el IDE, no relacionado) crear un skeleton en la ficha de socio para reemplazar "Cargando..." — misma estructura que la vista con datos, con cada sección en versión skeleton. Registrada como **T-20260826-004**, sin ejecutar hasta el `#run` explícito del turno siguiente.
- Al correr `#run T-20260826-004`, primer uso real del paso nuevo de `AGENTS.md` (T-20260826-003): `reducto query "features/member-detail-page/index.tsx" --resolve --view skeleton` — **confirmado que un path de archivo funciona directamente como query**, sin necesitar sintaxis especial. Cierra el gap que había quedado abierto en el gate de T-003.
- Leído el archivo completo (`Read`, tras el resolve de reducto) para tener el JSX exacto a replicar. `open_question` de la tarea resuelta sin bloquear: no existía `Skeleton` en `components/ui/` — se creó uno (primitivo estándar shadcn, mismo patrón `cn()` que `Button`/`Input`), documentado como decisión propia en el gate.
- Construido `MemberDetailSkeleton` (`features/member-detail-page/components/member-detail-skeleton.tsx` + `.module.css`): reutiliza los componentes `Table` reales y todo el texto estático (títulos de sección, labels, encabezados de columna), reemplazando por `<Skeleton>` solo lo que depende de datos — nombre, 6 valores del `dl`, botones de acción (dependen de props no disponibles aún), 3 filas placeholder por tabla. Se reusó `Table` (no un `<div>` a mano) a propósito para heredar su radio ya cappeado y no reintroducir el bug de contenedores-óvalo de T-20260825-010. `index.module.css` perdió `.loadingText`, sin uso tras el cambio.
- `tsc --noEmit`, `eslint features/member-detail-page components/ui/skeleton.tsx` y el detector mecánico de `impeccable` (paso obligatorio tras UI): los 3 limpios. `test:unit`: **34/34 verde**, sin el `PostgresError` de pool que venía apareciendo intermitentemente.
- `reducto context` post-tarea: **83% de tokens ahorrados** en la sesión (9 queries, 6.026 usados vs. 35.130 sin Reducto) — primera confirmación práctica de que T-20260826-003 funciona de punta a punta.
- Gate `medium`/async abierto en `graph/gates/pending/T-20260826-004.md`, sin aprobar todavía. **Gaps señalados:** sin verificación visual en navegador (mismo gap estructural de siempre); las otras 6 pantallas del proyecto que también muestran "Cargando..." quedan sin skeleton, fuera del alcance literal de esta tarea; grafo (`knowledge/`) sin reindexar tras los 3 archivos nuevos.

## Sesión 2026-08-26 (cont.) — `#task` + `#run T-20260826-005`: skeleton en `plans-page`
- Usuario pidió "lo mismo con plans" — misma lógica de skeleton, reemplazar "Cargando..." con la estructura real de `plans-page`. Registrada como **T-20260826-005**, sin ejecutar hasta el `#run` explícito.
- Al leer `features/plans-page/index.tsx` se confirmó una diferencia estructural con T-20260826-004: acá `loading` es un condicional inline (no un early-return) — el header (`Planes` + botón "Nuevo plan") ya se renderiza siempre, real e interactivo, sin depender de `plans`. Solo el área de la grilla necesitaba skeleton.
- `reducto query "features/plans-page/index.tsx" --resolve --view skeleton` antes de tocar código (paso de T-20260826-003, segunda vez que se aplica en la práctica).
- Construido `PlanCardSkeleton` (`features/plans-page/components/plan-card-skeleton.tsx` + `.module.css`): reusa `pageStyles.card` (el contenedor real, radio ya cappeado — el propio `index.module.css` de `plans-page` trae un comentario explícito sobre el bug óvalo de T-20260825-010, así que reusar la clase real en vez de reinventarla era la única opción correcta) y reemplaza por `<Skeleton>` los 4 elementos data-dependientes (`periodTag`/`cardName`/`cardPrice`/2 botones de `cardActions`). No hizo falta recrear el primitivo `Skeleton` — ya existía desde T-20260826-004, confirma que quedó reusable como se documentó en su gate.
- `index.tsx`: la rama `loading` pasa de `<p>Cargando...</p>` a 3 `<PlanCardSkeleton />` dentro del mismo `styles.grid` real.
- `tsc --noEmit`, `eslint features/plans-page`, detector de `impeccable`: los 3 limpios.
- `test:unit`: **23/34 verdes, 5 archivos fallaron** (`checkin-checkout`, `checkin-requires-active-membership`, `dashboard-metrics`, `member-soft-delete`, `payment-extends-membership`) — ninguno relacionado a `plans-page`. Síntoma distinto al `PostgresError 53300` habitual: esta vez un `TypeError` en el `afterAll` de `payment-extends-membership.test.ts` al borrar un tenant que nunca llegó a crearse, consistente con que el `beforeAll` falló contra la base antes del test. Tratado como el mismo patrón ambiental de sesiones anteriores sin investigar más — **señalado para revisión si se repite**, porque el síntoma cambió.
- `reducto context` post-tarea: 83% de tokens ahorrados — segunda tarea consecutiva confirmando T-20260826-003.
- Gate `medium`/async abierto en `graph/gates/pending/T-20260826-005.md`, sin aprobar todavía.

## Sesión 2026-08-26 (cont.) — diagnóstico + `#task` T-20260826-006: staff no puede entrar al dashboard
- Usuario reportó que el botón "Ir al dashboard" lo devuelve al index. Diagnóstico vía `reducto query --resolve` (sin `grep`/`Glob` — usuario recordó "usa reducto siempre", ya la segunda vez que lo pide en esta sesión, ver nota abajo): `HomePage` solo mira `getUser()` para mostrar el CTA (sin chequear rol); `middleware.ts` deja pasar porque el usuario tiene `tenant_id`/`role`; pero `app/(owner)/layout.tsx` hace `requireRole(context, ["owner"])` — cualquier rol que no sea `owner` cae en `ForbiddenError` → `redirect("/")`.
- Encontrada una contradicción ya existente en el código (no introducida ahora): `owner-nav.tsx` marca "Dashboard" como `ownerOnly: false` y `dashboard-page/index.tsx` (T-20260821-006) ya oculta el bloque de ingresos condicionado por rol — ambas señales asumen que `staff` debería poder entrar, pero `layout.tsx` lo bloquea igual. Gap ya documentado sin corregir en T-20260825-001 y T-20260825-005.
- Usuario confirmó registrar el fix. Registrada como **T-20260826-006**, sin ejecutar — el fix propuesto (`requireRole(context, ["owner", "staff"])`) queda señalado para revisar en el `#run`, porque el grupo `(owner)` agrupa TODAS las páginas (no solo dashboard) y hay que confirmar cuáles deberían seguir siendo owner-only antes de abrir el layout entero.
- **Feedback del usuario, memorizado:** preferencia explícita por usar `reducto query` en vez de `Grep`/`Glob` para exploración de código en este proyecto — ya lo había pedido una vez (sesión de T-20260826-002) y lo repitió acá. Guardado en memoria persistente para no tener que repetirlo una tercera vez.

## Sesión 2026-08-26 (cont.) — `#run T-20260826-006`: `staff` ya puede entrar al área `(owner)`
- Usuario confirmó ("si dale") aplicar el fix propuesto. Antes de tocarlo, se verificó con `Grep` (no `reducto` — búsqueda de string literal exacto `requireRole(` en varios archivos puntuales, un caso que Reducto no resuelve bien por ser semántico/fuzzy, no grep exacto) que `members`/`checkins`/`checkins/self`/`plans` (GET) ya eran `requireRole(["owner","staff"])` a nivel API, y que las únicas excepciones owner-only reales eran `plans` POST (pricing) y `GET /api/v1/staff` (roster, T-20260825-001) — ambas con su propio guard, intactas. `owner-nav.tsx` ya filtraba el link "Equipo" como `ownerOnly: true` y dejaba el resto abierto — la UI ya asumía este acceso.
- Se leyó el comentario original de `layout.tsx`: la restricción a `owner` fue una decisión deliberada en su momento ("out of scope for this phase"), no un descuido — pero quedó desactualizada por trabajo de sesiones posteriores (nav, dashboard condicionado por rol, APIs) que ya asumían lo contrario sin que nadie actualizara este guard.
- Aplicado: `requireRole(context, ["owner"])` → `requireRole(context, ["owner", "staff"])` en `app/(owner)/layout.tsx`, docstring reescrito para reflejar que las restricciones finas viven en cada API route, no acá. `tsc`/`eslint` limpios, `test:unit` **34/34 verde**.
- Gate `medium`/async abierto en `graph/gates/pending/T-20260826-006.md` — clasificado igual que los cambios UI puros (`edit_non_prod_file`) pese a ser autorización, porque es 100% reversible sin tocar datos/cuentas reales y las restricciones de negocio de verdad (pricing, roster) no se tocaron. **Gap señalado:** sin verificación funcional con una cuenta `staff` real (no hay sesión disponible) — el razonamiento se apoya en lectura de código, recomendado confirmarlo en vivo si el usuario tiene una cuenta de prueba.

## Sesión 2026-08-26 (cont.) — `#run T-20260826-007`: fichaje automático por login/logout
- Contexto encontrado al leer `tasks.md`: el usuario dictó un plan grande "Equipo" (T-007 a T-015) directamente en el archivo, fuera de esta conversación — cubre fichaje automático, alta de staff por administrativo, perfil propio de staff, necesidades operativas, cupos/reservas de clases, cobro rápido, leads, permisos por department, y rediseño de check-in. Varias marcadas `high` (crean cuentas o son schema+migración nueva) y dos (T-011, T-014) señaladas explícitamente como "no debería `#run`'earse sin scoping propio".
- Usuario pidió `#run` de T-007, T-008 y T-009 en ese orden. Antes de tocar nada se revisó `severity_flag` de cada una: T-007 `medium`, **T-008 `high`** (crea cuentas reales de Auth, mismo nivel que T-20260821-007/T-20260825-002 — requiere gate síncrono con aprobador nombrado ANTES de implementar, no después), T-009 `medium`.
- T-007: investigación previa (vía `Read`/`Grep` — el research necesitaba archivos completos y un grep de string literal exacto, no búsqueda semántica, casos que Reducto no cubre bien) de `sign-in-form.tsx`, `presence-widget.tsx`, `staff-attendance.ts`, las rutas manuales de fichaje (T-20260825-005) y `staff-members.ts` para entender la cadena `authUserId → users.id → staffMembers.id`. Construido `POST`/`PATCH /api/v1/staff/me/attendance` — self-service, nunca toma un `staffMemberId` externo, siempre resuelve "quién soy yo" de la sesión. `sign-in-form.tsx` dispara el clock-in fire-and-forget tras login exitoso; `presence-widget.tsx` hace `await` del clock-out **antes** de `signOut()` (orden crítico: necesita la cookie de sesión todavía viva). `staff-page/index.tsx`: botón manual de fichaje ahora exclusivo de `staffCategory === "cleaning"`.
- `tsc`/`eslint`/detector de `impeccable` limpios, `test:unit` **34/34 verde**. Gate `medium`/async abierto en `graph/gates/pending/T-20260826-007.md`. **Gap señalado:** sin prueba end-to-end con cuenta staff real (login→fila abierta→logout→fila cerrada) — el diseño es deliberadamente best-effort/silencioso ante fallos para no bloquear login/logout, lo que también significa que un bug real en la resolución fallaría sin avisar.
- Reindexado `reducto ingest .` tras la tarea (severidad `low`, sin gate).
- **Próximo paso:** T-20260826-008 requiere confirmación síncrona explícita del usuario antes de escribir código (ver pregunta en el chat) — no se implementó todavía.

## Sesión 2026-08-26 (cont.) — `#run T-20260826-008`: administrativo puede crear cuentas de staff
- Antes de tocar código: `AskUserQuestion` síncrona (severity_flag `high`, mismo nivel que T-20260821-007/T-20260825-002) — usuario respondió "Sí, correla".
- `app/api/v1/staff/route.ts` POST: `requireRole(["owner"])` → `requireRole(["owner","staff"])`, más un segundo gate explícito (staffCategory === "administrative") vía `resolveOwnStaffMember()`, porque `requireRole()` no conoce `staffCategory`, solo `role`. Extraído `lib/staff/resolve-own-staff-member.ts` como helper compartido — refactorizado T-007 (`staff/me/attendance/route.ts`) para usar el mismo helper en vez de tener la query duplicada inline.
- **Gap deliberado, no resuelto:** `GET /api/v1/staff` y el nav "Equipo" siguen owner-only (T-20260825-001) — un administrativo puede llamar el POST directo pero no tiene botón "Invitar" visible todavía. No se tocó porque es terreno de T-20260826-014 (permisos por department/categoría), que el propio backlog reserva para su propia discusión con `backend-developer` antes de tocarla.
- `tsc`/`eslint` limpios, `test:unit` **34/34 verde**. Gate `high`/síncrono en `graph/gates/pending/T-20260826-008.md`, **ya marcado aprobado** (aprobación ocurrió en el chat antes de implementar, no después).
- Reindexado `reducto ingest .`.

## Sesión 2026-08-26 (cont.) — `#run T-20260826-009`: "Mi perfil" para staff
- Diseño: `GET`/`PATCH /api/v1/staff/me` (nuevo) para datos propios (teléfono, y certificaciones solo si `staffCategory === "instructor"`), reusando/extendiendo `resolveOwnStaffMember` (T-007/T-008) al shape completo. Email y password van directo del cliente a `supabase.auth.updateUser()`, sin pasar por API propia — misma lógica que sign-in/sign-out (es la sesión del propio usuario, no necesita el admin client de Supabase).
- **Decisión de scope, no construir de más:** "clases" en el perfil de profesor no es un editor nuevo — el usuario ya había confirmado que el alcance es idéntico al que `ScheduleFormDialog` tiene hoy (owner+staff sin distinción de categoría), así que la sección "Clases" del perfil solo linkea a `/schedules` en vez de duplicar el editor.
- `app/(owner)/profile/page.tsx` (nuevo) hace el guard server-side (redirige a `/dashboard` si no es staff o si es `cleaning`), mismo patrón que `layout.tsx`. Para que el nav supiera mostrar "Mi perfil" solo a quien corresponde, `owner-nav.tsx` pasó de un booleano `ownerOnly` por link a una función `visible(user)` — refactor mecánico de los 6 links existentes, mismo resultado, más expresivo para el caso nuevo (staff-pero-no-cleaning). `PresenceUser` ganó `category`, resuelta en `layout.tsx` solo para staff.
- **Gap real detectado y documentado, no resuelto:** el cambio de email de Supabase manda confirmación a la dirección nueva y no aplica al instante; este proyecto no tiene ninguna ruta `/auth/callback`. Supabase igual termina aplicando el cambio del lado de Auth una vez confirmado, pero la columna espejo local `users.email` no se resincroniza sola — queda desactualizada hasta que algo la vuelva a tocar. Señalado en el código (docstring del componente) y en el gate en vez de construir un webhook/callback que nadie pidió.
- `tsc` limpio, `eslint .` (repo completo) limpio, detector de `impeccable` limpio, `test:unit` **34/34 verde**. Gate `medium`/async abierto en `graph/gates/pending/T-20260826-009.md`.
- Reindexado `reducto ingest .`. **Con esto, las 3 tareas pedidas por el usuario en esta tanda (`#run T-007`, `T-008`, `T-009`) quedan completadas** — backlog vuelve a 0 en curso, quedan T-010 a T-015 pendientes sin correr.

## Sesión 2026-08-26 (cont.) — `#run T-20260826-015` bloqueada por dependencias
- Usuario pidió `#run T-015`. Validadas sus `depends_on` (`T-20260826-011`, `T-20260826-012`) contra `tasks.md`: **ninguna de las dos está completada** — ambas siguen en "Tareas pendientes", sin `#run` todavía. Por la Backlog Execution Policy de `AGENTS.md`/`tasks.md` ("si una dependencia está pendiente... la tarea queda bloqueada, frená la ejecución, no continúes con tareas downstream"), se frenó sin tocar código y se avisó al usuario en el chat en vez de ejecutar igual.
- No se movió T-015 de "pendiente" a "en curso" ni se le agregó rastro de ejecución — sigue intacta en `tasks.md`, solo se registra acá el intento bloqueado.

## Sesión 2026-08-26 (cont.) — `#run T-011 y T-012`: scoping + T-011 (cupos/reservas de clases)
- Usuario pidió correr T-011 y T-012 juntas. Antes de tocar código: `PRODUCT.md` ya resolvía una de las 3 open_questions de T-011 ("members have no login" — reservas siempre las carga staff). Para las otras 2, se convocó `uiux-designer`+`backend-developer` en paralelo (background), tal como pedía la propia tarea. Mientras corrían, se resolvieron las 2 open_questions de T-012 directo con el usuario vía `AskUserQuestion`: sin control de stock (catálogo simple), pase diario como concepto nuevo y liviano (no reusar membership).
- Los dos agentes convergieron independientemente en la misma recomendación: (1) cupo por ocurrencia real con fecha, derivada de `(class_schedule_id, date)`, materializada lazy (nunca batch/cron); (2) promoción de lista de espera manual, nunca automática por tiempo (este proyecto no tiene infraestructura de cron frecuente, y automatizarlo resolvería un problema hipotético en vez del real).
- Construido T-011 completo: schema (`class_schedules.capacity`, tablas nuevas `class_occurrences`/`class_reservations`, enum `reservation_status`, RLS `0008_class_occurrences_reservations_rls.sql`), API (`GET/POST/PATCH` bajo `/api/v1/schedules/[id]/occurrences/**`, con promoción atómica de waitlist en la misma transacción que libera un cupo), UI (`ClassOccurrenceDialog` nuevo, trigger "Reservas" en `ScheduleTableView` solamente, campo de cupo en `ScheduleFormDialog`). Índice único **parcial** en `class_reservations` (primera vez que se usa esa forma en el proyecto) para permitir recancelar/re-reservar sin bloquear para siempre.
- `npm run db:generate` corrido (`0014_lazy_scarlet_witch.sql`, revisada a mano) — **`npm run db:migrate` NO se corrió**, mismo criterio `high`/síncrono que toda migración anterior de este proyecto. `tsc`/`eslint .` limpios; detector de `impeccable` con 1 advisory preexistente no relacionado (`schedule-calendar-view.module.css`, no tocado); `test:unit` 26/34, 5 fallas de integración de DB ambientales, no relacionadas.
- Gate `high`/síncrono abierto en `graph/gates/pending/T-20260826-011.md`, sin aprobar — queda esperando confirmación explícita antes de aplicar la migración. Sigue con T-012 a continuación.

## Sesión 2026-08-26 (cont.) — T-012 (cobro rápido/kiosco)
- Con las 2 open_questions de T-012 ya resueltas antes de T-011 (sin control de stock; pase diario como concepto nuevo y liviano), se construyó completo: `products` (catálogo sin stock, bloqueado de borrar si tiene ventas vía FK restrict — mismo 409 que `plans`→`memberships`) y `walk_in_sales` (append-only como `payments`, unifica venta de producto y pase diario bajo un `kind` enum; el monto de una venta de producto lo deriva el servidor del precio actual del catálogo, nunca confía en el monto que mande el cliente).
- Permisos calcados de `plans`: vender es owner+staff (mostrador), pero gestionar el catálogo (pricing) y ver el total de ventas (`GET /walk-in-sales`, dato de facturación) son owner-only — mismo criterio que T-20260821-006 ocultando ingresos a staff.
- UI nueva "Cobro rápido" (`/kiosk`, link agregado al nav para todos): grilla de productos con botón "Vender" de un tap (sin carrito — no se pidió un POS de varios ítems), sección de pase diario (nombre opcional + monto libre), y solo para owner "Ventas de hoy" + gestión del catálogo.
- `npm run db:generate` corrido (`0015_stormy_moonstone.sql`, revisada a mano) — **`npm run db:migrate` NO se corrió**, mismo criterio `high`/síncrono. `tsc`/`eslint .` limpios, detector de `impeccable` sin advisories nuevos, `test:unit` **34/34 verde** (el pool de conexiones se recuperó esta vez, sin fallas ambientales).
- Gate `high`/síncrono abierto en `graph/gates/pending/T-20260826-012.md`, sin aprobar. **Con esto, las 2 tareas pedidas en esta tanda (`#run T-011 y T-012`) quedan completadas** — código listo, ambas migraciones generadas y esperando la misma aprobación síncrona antes de aplicarse a la base real. T-015 (rediseño de Check-in) queda técnicamente desbloqueada, pero no se corrió — no fue pedida.

## Sesión 2026-08-26 (cont.) — `#run-all`: T-010 (necesidades operativas) + T-013 (leads) en paralelo, luego aprobación y aplicación conjunta de 4 migraciones
- Usuario pidió `#run-all`. Backlog pendiente en ese momento: T-010, T-013, T-014, T-015 (T-011/T-012 ya completadas en sesión previa, código sin migrar). Validado `depends_on` de las 4 contra `tasks.md`: todas dependen solo de T-006 (completada), sin bloqueo formal de grafo.
- T-014 y T-015 tenían decisiones marcadas explícitamente como "no asumir" dentro de la propia tarea (matriz de permisos de ventas/contabilidad/gerencia sin definir; timing de construir UI sobre datos que no existen en la base real todavía) — se preguntaron al usuario vía `AskUserQuestion` antes de tocar código ahí. T-010 y T-013 no tenían open_questions bloqueantes, así que se lanzaron de inmediato.
- **T-010 y T-013 ejecutadas vía 2 subagentes `backend-developer` en paralelo (background)**, sobre archivos disjuntos, con instrucción explícita de NO editar `tasks.md`/`progress.md` (para evitar conflicto de escritura concurrente) y de generar migración sin aplicarla. Ambas completadas limpias (`tsc`/`eslint`/`impeccable`/`test:unit` 34/34 cada una) — detalle completo en sus entradas de `tasks.md`.
- **Efecto secundario no anticipado de correr generate en paralelo:** como ambos agentes corrieron `npm run db:generate` con el schema del otro ya wireado en disco, `drizzle-kit` produjo **una sola migración compartida** (`0016_soft_ender_wiggin.sql`) con las tablas de las dos tareas (`leads` + `operational_requests`). Ambos agentes lo detectaron y lo documentaron por su cuenta en sus respectivos gates, coincidiendo en no separarlo a mano (más riesgoso que dejarlo consolidado, podría desincronizar el snapshot de drizzle).
- Usuario había pedido antes aplicar solo las migraciones de T-011+T-012 para poder construir T-015 sobre datos reales. Se le señaló que `npm run db:migrate` (`drizzle-kit migrate && tsx scripts/apply-policies.ts`) aplica **todo lo pendiente de una sola pasada** — no existe forma de aplicar 0014/0015 sin arrastrar también 0016 (T-010+T-013, recién generada). Usuario confirmó vía `AskUserQuestion` "aprobar las 4 juntas".
- Se revisaron a mano los 4 gates (`T-20260826-010/011/012/013.md`) y el contenido del SQL de las 3 migraciones (`0014`, `0015`, `0016`) antes de aplicar — consistentes con lo documentado, sin hallazgos. Se marcaron los 4 gates como aprobados (`Aprobado por: SudacaDev`, `Fecha: 2026-08-26`, vía `AskUserQuestion` — mismo mecanismo de aprobación que T-20260826-008).
- `npm run db:migrate` corrido una sola vez: `drizzle-kit migrate` aplicó `0014`-`0016` sin errores; `apply-policies.ts` aplicó las 11 policies (`0001`-`0011`, notices esperados de `DROP POLICY IF EXISTS` en las 4 nuevas por ser la primera vez que corren). Verificado contra la base real con una query directa a `pg_tables`: `class_occurrences`, `class_reservations`, `leads`, `operational_requests`, `products`, `walk_in_sales` — las 6 tablas nuevas de esta sesión, todas con `rowsecurity: true`.
- **Con esto quedan completadas T-010, T-011, T-012 y T-013**, las 4 con migración aplicada y verificada. Backlog pendiente restante: T-014 (permisos por department, alcance limitado a "solo recepción ahora" por decisión del usuario) y T-015 (rediseño Check-in, ahora con datos reales disponibles — "aplicar migraciones primero" ya resuelto).

## Sesión 2026-08-26 (cont.) — `#run-all`: T-014 (permisos de recepción) — auditoría, sin código de gating nuevo
- Con T-010/011/012/013 ya completadas y migradas, se lanzó T-014 vía subagente `backend-developer` (background), con el alcance ya acotado por el usuario a "solo recepción" (ventas/contabilidad/gerencia quedan para más adelante, confirmado vía `AskUserQuestion` antes de arrancar).
- El agente confirmó primero el modelo: `staff_members.department` solo aplica a `staffCategory: "administrative"` (instructores/limpieza nunca lo tienen). Después auditó las 5 reglas "NO puede" de recepción contra el código real de cada endpoint candidato — **las 5 ya estaban cubiertas**: 3 por ser owner-only (`plans` crear/editar, `products` editar precio, `walk-in-sales` GET + dashboard de ingresos) y 2 por no existir el feature en absoluto (`payments` es append-only sin DELETE; no hay ningún concepto de payroll/liquidación de sueldos en el schema). Las 6 reglas "SÍ puede" también ya funcionaban sin cambios para cualquier `staff`. "Abrir caja" tampoco existe como feature real (solo copy de marketing).
- **Resultado: no hizo falta ningún gate de autorización nuevo.** El único cambio de código es un comentario en `db/schema/enums.ts` documentando el audit, para que una futura tarea sobre ventas/contabilidad/gerencia no tenga que re-derivar esta investigación.
- Antes de aprobar el gate `high`/síncrono que el agente dejó pendiente, se verificó a mano (no solo confiando en el reporte del subagente) el código real de los 3 puntos de mayor riesgo (`plans/[id]/route.ts`, `walk-in-sales/route.ts`, `dashboard-page/index.tsx`) — coincidieron exactamente con lo reportado. Gate aprobado con esa verificación como respaldo.
- `tsc`/`eslint .` limpios, `test:unit` 34/34. T-20260826-014 completada.

## Sesión 2026-08-26 (cont.) — `#run-all`: T-015 (rediseño Check-in a 3 columnas) — cierra el `#run-all`
- Último ítem del backlog pedido por `#run-all`. Corrido vía subagente `frontend-developer` (background) en paralelo con T-014, ya con los datos de T-011/T-012 disponibles en la base real.
- `/checkin` pasa de una columna a 3: "Actividad de hoy" (conserva el check-in original + feed de hoy con timestamp relativo, nuevo endpoint `?today=true` aditivo en `app/api/v1/checkins/route.ts`), "Clase actual" (resuelve qué `class_schedule` está en curso por día+hora local, reusa el endpoint de ocurrencias de T-011 con presente/ausente), "Punto de venta rápido" (grilla compacta reusando el patrón de `/kiosk`, sin gestión de catálogo).
- Decisión técnica documentada: la resolución de "hoy" para la clase en curso usa fecha **local**, a propósito distinto del patrón UTC que usa `schedules-page/lib/day-labels.ts`, para evitar el desfase de 3hs que produciría leer "mañana" desde las 21:00 en Argentina si se reusara ese mismo patrón acá.
- `tsc`/`eslint .` limpios, `impeccable` sin hallazgos, `test:unit` 23/34 con 0 fallas de aserción (resto son fallas ambientales de pool de conexión, ya documentadas repetidas veces en esta sesión).
- Gate `medium`/async en `graph/gates/pending/T-20260826-015.md`, sin aprobar todavía (no bloqueante, mismo tratamiento que otros gates async de esta sesión).
- **Con esto se completa el `#run-all` pedido por el usuario: T-010, T-011, T-012, T-013, T-014 y T-015 quedan todas completadas.** Backlog de `tasks.md` sin tareas pendientes ni en curso al cierre de esta sesión.

## Sesión 2026-08-27 — `/impeccable critique` + `polish`: Cobro rápido, Socios, Equipo, Prospectos, Necesidades
- Usuario compartió 5 capturas en vivo y dijo directamente "no me gusta el diseño en sí" / "¿entregarías un programa así?", comparando contra `/schedules` (Horarios), la única pantalla que sí le gusta. Confirmó el flujo `impeccable critique` → `polish` vía `AskUserQuestion`.
- **Critique:** doble sub-agente aislado obligatorio (Assessment A = revisión de diseño vía Nielsen/cognitive-load/personas; Assessment B = `detect.mjs` + evidencia de navegador). Ambos sub-agentes chocaron con el mismo bloqueo estructural de siempre: sin sesión autenticada, las 5 rutas redirigen a `/sign-in` — critique 100% source-grounded, no confirmado visualmente. `detect.mjs` dio 0 hallazgos en las 5 carpetas (confirmado genuino con `--no-config` + control positivo) — se documentó explícitamente que el tipo de problema encontrado (composición, no CSS-syntax) queda fuera del alcance de un scanner mecánico. Score final: **19/40 (Poor)**. Reporte completo entregado en el chat y persistido en `.impeccable/critique/2026-08-27T02-48-22Z__features-kiosk-page.md` (slug `features-kiosk-page`, cubre las 5 pantallas). Usuario eligió alcance "las 5 pantallas" y prioridad "todo, en el orden del reporte" vía `AskUserQuestion`.
- Antes de implementar, usuario preguntó si necesitaba mockups previos (como el del fix de radius). Se le aclaró que el paso #1 (reemplazar `confirm()`) reusa el `Dialog` ya existente sin decisión visual nueva, y para los pasos #2/#3 (sí con layout nuevo) se ofreció mostrar un Artifact antes de tocar código — usuario eligió "seguí sin frenar" y revisar en vivo al final.
- Usuario preguntó si correr los 6 fixes "todas juntas" generaba conflicto — se le explicó que sí (Socios/Prospectos/Necesidades/Equipo son tocados por 2-3 fixes cada uno) y se corrieron **secuencialmente**, cada uno partiendo del estado ya actualizado por el anterior, con `tsc`/`eslint`/`detect.mjs` limpios después de cada paso:
  1. `components/ui/confirm-dialog.tsx` (nuevo) reemplaza `window.confirm()` en `useMembers.ts`/`useStaff.ts`.
  2. `GET /api/v1/members` joinea estado de membresía (mismo cálculo `lib/memberships/status.ts` que ya usa el dashboard) → columna Estado + orden por defecto por urgencia (`features/members-page/types.ts`).
  3. Kiosco separado en panel "Vender" (tiles grandes, todo el tile es el tap target) vs. panel "Catálogo" (owner-only, tabla compacta) — ataca el mis-tap Vender/Borrar señalado en el critique.
  4. `components/ui/empty-state.tsx` (nuevo) aplicado en Socios/Equipo/Prospectos/Necesidades, preservando en Equipo la distinción "sin datos" vs. "sin resultados de búsqueda".
  5. Búsqueda agregada a Socios y Necesidades (Prospectos ya tenía tabla, se le sumó búsqueda + orden "nuevo primero"), reusando el patrón de Equipo.
  6. Polish: focus-visible faltante en los tiles de Kiosco, barrido final de `tsc`/`eslint`/`detect.mjs`/`vitest` sobre todo lo tocado.
- `test:unit`: 49/49 en dos corridas completas de la suite (sin la flakiness de conexión documentada otras veces en esta sesión).
- Detalle completo, contexto y paths exactos: ver `tasks.md`, tarea `T-20260827-001`.
- **Gap conocido, sin cambios respecto a sesiones anteriores:** ninguna verificación visual real en navegador fue posible (ni para el critique ni para el polish) — no hay sesión autenticada disponible para esta sesión de Claude.
- **Sin commitear al cierre de esta entrada** — 16 archivos modificados + 4 nuevos en el working tree, más algunos cambios previos ya presentes (la excepción de `<textarea>` en `DESIGN.md`/2 `form-dialog.module.css`, sin relación con este trabajo) que tampoco se commitearon.

## Sesión 2026-08-27 (cont.) — DB connection pool agotado (real, no ambiental) + 2 bugs de radio + gaps de UX en `/checkin` + `#run T-20260827-002`
- Usuario reportó `PostgresError: remaining connection slots are reserved for roles with the SUPERUSER attribute` en vivo. Diagnóstico: `.env.local` conecta directo a Postgres (`:5432`, no el pooler de Supabase) y `db/client.ts` cacheaba el pool de conexiones en una `let` de módulo — cada recarga de HMR en dev creaba un pool nuevo (hasta 20 conexiones) sin cerrar el anterior, agotando el límite real tras una sesión larga. Esto explica retroactivamente las fallas "ambientales" de `PostgresError`/pool agotado documentadas repetidas veces en tests de sesiones anteriores — no eran ambientales, eran este bug. **Fix:** `db/client.ts` ahora cachea el pool en `globalThis`, sobrevive a HMR. No libera las conexiones ya colgadas — se le indicó al usuario reiniciar el dev server para eso. Se sugirió (no aplicado, decisión de infra del usuario) migrar a el pooler de Supabase (`:6543`).
- Usuario reportó un `<textarea>` con el radio pill completo sin capar (mismo bug documentado en T-20260825-010, pero en un componente nuevo de hoy). Corregido en `operational-request-form-dialog.module.css` y, al ser el mismo CSS copiado, también en `member-form-dialog.module.css` (notas de salud) — mismo bug no reportado ahí pero presente. `DESIGN.md` actualizado con una excepción explícita: `<textarea>` cuenta como contenedor multi-fila para el radio, no como control de línea única.
- Usuario pidió que "Editar"/"Borrar" del catálogo en `/kiosk` pasen de texto a ícono (desbordaban el contenedor de la tabla) — corregido con `PencilIcon`/`Trash2Icon`, `size="icon-xs"`, `aria-label`.
- Usuario probó "Presente" en la columna "Clase actual" de `/checkin` y reportó que el socio "se sacaba de la clase" — gap real de T-20260826-015: esa columna nunca tuvo sección "Historial" (a diferencia del mismo diálogo en `/schedules`), así que una reserva marcada `attended` desaparecía de la vista sin dejar rastro visible. Agregada la sección "Historial" a `current-class-column.tsx`/`useCurrentClass.ts`, mismo agrupamiento que `class-occurrence-dialog.tsx`.
- Usuario pidió agregar reserva directo desde "Clase actual" (antes exclusivo de `/schedules`, decisión de alcance original de T-015) — agregado el mismo patrón de búsqueda de socio + "Reservar" que ya existía en `class-occurrence-dialog.tsx`.
- Usuario compartió una captura de `/checkin` funcionando con datos reales (confirma T-011/T-012/T-015 operativos) y dio dos pedidos más:
  1. **`#task` T-20260827-002** — feedback real de carga: botones sin loader/texto de pendiente + reemplazar todo "Cargando..." plano por skeleton estructural. Registrada, no ejecutada en el momento (protocolo `#task`).
  2. **`#task` T-20260827-003** — nombre de socio en `/checkin` clickeable → `/members/[memberId]` para agregar membresía. Verificado antes de registrar que `membership-form-dialog.tsx` y la ruta ya existen — el trabajo real es solo navegación. Registrada, no ejecutada.
- **`#run T-20260827-002`**: dividida en 4 lotes paralelos por carpetas disjuntas (`schedules-page`; `checkin-page`; `members-page`+`staff-page`+`member-detail-page`; `leads-page`+`operational-requests-page`+`kiosk-page`+`profile-page`), cada uno advertido explícitamente sobre el trabajo de la sesión de critique/polish (T-20260827-001) que había corrido horas antes sobre varias de las mismas carpetas, para que releyeran el estado actual en vez de asumir. Los 4 terminaron limpios (`tsc`/`eslint`/`impeccable`/`test:unit` por lote), con varios hallazgos propios fuera del pedido literal pero dentro del criterio abierto de la tarea: botón "Borrar" de horarios sin feedback (schedules), botones de estado de membresía sin feedback (member-detail), botón "Borrar" ícono-only de catálogo resuelto con spinner en vez de texto (kiosk), panel "Catálogo" de kiosk sin ningún loading propio (agregado, no pedido literalmente). Verificación final consolidada (`tsc`/`eslint`/`test:unit` sobre el repo completo tras los 4 lotes): limpia, **34/34 verde**. Detalle completo y paths exactos: ver `tasks.md`, tarea `T-20260827-002`.
- **Gates:** 4 nuevos `medium`/async (`T-20260827-002-{schedules,checkin,members-staff,leads-requests-kiosk-profile}.md`), sin aprobar todavía, no bloqueantes.
- **Sin commitear al cierre de esta entrada** — cambios de esta sesión más los de T-20260827-001 (sesión de critique/polish) conviven en el mismo working tree sin conflictos confirmados (verificación final corrida sobre todo junto).

## Sesión 2026-08-27 (cont.) — `#run T-20260827-004`: Editar/Borrar de Horarios (vista Tabla) a solo-ícono
- Mismo criterio que el fix de `/kiosk` (T-20260827-002): `PencilIcon`/`Trash2Icon` en vez de texto, con `Loader2Icon` girando durante el borrado (preserva el feedback "Borrando..." como spinner). Se mantuvieron los `<button>` planos existentes (`.slotAction*`) en vez de migrar a `Button`/`icon-xs` — son un link-button minimalista de 11px, consistente con "Reservas" (que queda en texto, no pedido). `tsc`/`eslint` limpios. No aplicado al Kanban (mismo patrón ahí) — el usuario pidió específicamente la tabla.

## Sesión 2026-08-27 (cont.) — Build roto: espacio dentro de `min(var(--radius),N px)` en `schedules-page/index.module.css`
- Usuario reportó `Syntax error: tailwindcss: Cannot apply unknown utility class` en `.weekWrap`. Causa: una edición concurrente (no de esta sesión) le agregó un espacio después de la coma en 3 lugares (`min(var(--radius-xl), 20px)` / `min(var(--radius-lg), 20px)`) — Tailwind no tolera espacios dentro de un valor arbitrario entre corchetes, el espacio corta el token de clase ahí mismo. Convención establecida en todo el resto del proyecto (`table.tsx`, `dialog.tsx`, `button.tsx`, etc.) es sin espacio — corregido para volver a esa forma. Confirmado con `next build`: compila limpio.
- `grep` sobre todo el proyecto confirmó que era el único archivo afectado.

## Sesión 2026-08-27 (cont.) — `#task` T-20260827-005: fix de raíz del bug de build recurrente (`rounded-[min(var(--radius-X),Ypx)]`)
- El fix puntual de `schedules-page/index.module.css` (sesión anterior de esta misma conversación) **se rompió solo dos veces más** mientras el usuario seguía con el archivo abierto — confirmado en vivo por las notas de "changed on disk" entre mensajes: algo en su editor reformatea el CSS al guardar y reintroduce el espacio después de la coma en `min(var(--radius-X), Ypx)`, y el corchete de valor arbitrario de Tailwind no tolera ese espacio. Ofrecidas 3 opciones vía `AskUserQuestion` (fix de código eliminando la coma / config de editor / dejarlo así); eligió el fix de código.
- Agregados 6 tokens `--radius-cap-{base}-{px}` a `app/globals.css` (`@theme inline`), cada uno pre-resolviendo exactamente el `min(...)` que reemplaza (sin consolidar combinaciones base/px distintas, para no cambiar semántica futura si `--radius` deja de ser pill algún día). Tailwind v4 genera automáticamente `rounded-cap-{base}-{px}` (+ variantes direccionales) desde cada token, mismo mecanismo que `rounded-lg`. Reemplazo mecánico vía `sed` en los 22 archivos del proyecto que usaban la forma vieja con corchetes — no solo `schedules-page`, era un patrón repetido en `components/ui/*` y ~15 features más.
- Verificado de punta a punta: `tsc`/`eslint` limpios, **`npm run build` completo sin errores** (36/36 rutas, cache `.next` limpiada primero para descartar falsos positivos), CSS compilado inspeccionado a mano confirmando el mismo valor final (`min(var(--radius-xl),20px)`), `test:unit` 34/34.
- `DESIGN.md` actualizado con la convención nueva (`rounded-cap-*`) y la razón de por qué existe, para que ninguna tarea futura vuelva a escribir la forma con corchetes+coma.
- No se tocó la configuración del editor del usuario (causa raíz del reformateo) — el fix la vuelve irrelevante para este patrón específico, pero no la elimina como riesgo general si alguien escribe un `rounded-[...]` nuevo a mano.

## Sesión 2026-08-27 (cont.) — `#task` T-20260827-007: instructor por clase, pendiente de gate `high`
- Usuario preguntó "¿los profesores dónde cargan sus clases?" tras el trabajo de critique+polish (T-001). Se investigó `db/schema/class-schedules.ts` y se confirmó que v1 no tiene concepto de instructor (decisión de diseño documentada ahí desde T-20260821-004) — cualquier staff (incluidos profesores) puede técnicamente entrar a `/schedules` y cargar horarios, pero nada asocia una clase a quién la da, y "Mi perfil" no tiene ningún vínculo con horarios.
- Usuario pidió explícitamente: "el profesor si quiere ver su clase (que se la muestra la recepcionista) puede cargar su horario y ver cuantas personas hay anotadas en la clase." Antes de tocar código (esto toca schema), se presentó una propuesta concreta en el chat y se confirmaron 2 decisiones vía `AskUserQuestion`: (1) el profesor se autoasigna al cargar, owner/administrativo puede asignar a cualquiera; (2) reusar el diálogo de reservas que ya existe (`ClassOccurrenceDialog`, T-011) en vez de construir una pantalla nueva para ver el cupo.
- **Hallazgo importante durante la implementación:** al generar la migración y correr `test:unit`, se detectó que `tasks.md` ya tenía entradas `T-20260827-002` a `-006` de una sesión concurrente (loading skeletons + otro `#task` de "profesor activo en /checkin") — la tarea se había registrado internamente como "T-20260827-002" en varios comentarios de código antes de notar la colisión de IDs. Se corrigió con `sed` a `T-20260827-007` (el siguiente ID libre) en los 7 archivos afectados, verificado con `tsc`/`eslint` limpios después del fix.
- **Ese mismo backlog concurrente reveló algo relevante:** T-20260827-006 (otra sesión, mismo día) evaluó agregar esta misma columna de instructor para un pedido distinto ("mostrar qué profesor está activo en /checkin") y el usuario **la descartó explícitamente para ese caso**, prefiriendo "instructores fichados ahora, sin vínculo a la clase". No es una contradicción — son dos pedidos distintos del mismo usuario, cada uno resuelto según lo que pidió específicamente — pero se documentó el cruce en el gate (`T-20260827-007.md`) para que quien apruebe tenga el contexto completo.
- Implementado: columna `instructorId` (nullable, FK a `staff_members`, `onDelete: set null`) en `class_schedules` + relación en `relations.ts`; `lib/schedules/resolve-instructor-id.ts` (nuevo) fuerza autoasignación server-side para `staffCategory: "instructor"` en POST/PATCH de `/api/v1/schedules`, ignorando lo que mande el cliente — no se puede spoofear; `app/api/v1/staff/instructors/route.ts` (nuevo) expone un picker mínimo (`{id,firstName,lastName}`) deliberadamente más angosto que `GET /api/v1/staff` (owner-only desde el gate `T-20260825-001` — esta ruta nueva no expone nada de lo que esa restricción protegía). En `features/schedules-page`: picker de instructor en `ScheduleFormDialog` (reemplazado por una nota estática "Vos (Nombre)" cuando quien mira es el propio profesor, para no mostrar un selector que en los hechos está bloqueado server-side), filtro "Mis clases" en las 3 vistas (tabla/kanban/calendario, filtrado antes de pasar `schedules` a cada una — no hizo falta tocar el render interno de ninguna), hook `useOwnStaffMember` nuevo reusando `GET /api/v1/staff/me` (tolerante al caso owner, que da 404 ahí — no es un error a mostrar).
- Migración generada (`db/migrations/0017_stormy_felicia_hardy.sql`, solo `ALTER TABLE ADD COLUMN` + FK) pero **no aplicada** — gate `high`/sync abierto en `graph/gates/pending/T-20260827-007.md`, sin aprobar todavía. `test:unit` corrido igual: 34/34 + 15 skipped, con `rls-policies.test.ts` fallando exactamente donde se esperaba (`column "instructor_id" ... does not exist` contra la base real) — confirmado que es el estado esperado pre-migración, no una regresión.
- `tsc --noEmit`, `eslint .`: limpios. Detector de `impeccable` sobre todo lo tocado: limpio (1 advisory preexistente sin relación en `schedule-calendar-view.module.css`, ya documentado por la sesión concurrente en T-20260827-002).
- **Gap conocido:** el filtro "Mis clases" en Kanban/Calendario no tiene forma de abrir el diálogo de reservas — solo la vista Tabla lo tiene (limitación preexistente de esas dos vistas, no introducida acá; el profesor cae en Tabla por default). Sin verificación visual en navegador, mismo gap estructural de siempre.
- **Pendiente para la próxima sesión o para el usuario:** aprobar el gate `T-20260827-007` (`npm run db:migrate`) para que el feature funcione contra datos reales — sin eso, la UI está lista pero cualquier alta/edición de horario con instructor fallará contra la base real.

## Sesión 2026-08-27 (cont.) — gate `T-20260827-007` aprobado y aplicado: `/schedules` recuperado
- Usuario reportó en vivo "che no me levanta los horarios" — exactamente el efecto que el gate recién abierto anticipaba: el código ya esperaba `class_schedules.instructor_id` (schema de Drizzle actualizado), pero la base real todavía no tenía la columna, así que `SELECT` fallaba contra producción con `column "instructor_id" does not exist`. Se explicó la causa y se pidió aprobación inmediata para aplicar la migración pendiente. Usuario aprobó: "si lo apruebo".
- `npm run db:migrate` corrido: `drizzle-kit migrate` aplicó `0017_stormy_felicia_hardy.sql` sin errores; `apply-policies.ts` reaplicó las 11 policies existentes (notices esperados de `IF EXISTS`, ninguna nueva necesaria — es solo una columna en una tabla que ya tenía RLS). Verificado contra la base real con una query directa a `information_schema.columns` y `pg_constraint`: columna `instructor_id` (`uuid`, `nullable`) y el FK correspondiente, ambos presentes.
- `npm run test:unit` corrido de nuevo: **49/49 verde** — `rls-policies.test.ts`, que antes fallaba exactamente por la columna faltante, ahora pasa completo.
- Gate `T-20260827-007.md` actualizado con la aprobación (`SudacaDev`, 2026-08-27) y el resultado verificado. `tasks.md` actualizado: la tarea pasa de "Tareas pendientes" (bloqueada por el gate) a "Tareas completadas".
- Con esto, `/schedules` y el feature de instructor por clase (autoasignación, picker owner/administrativo, filtro "Mis clases", cupo vía el diálogo de reservas existente) quedan funcionales contra datos reales.

## Sesión 2026-08-27 (cont.) — `#task` T-20260827-008: "Fichar profesor" desde /checkin
- Usuario preguntó "¿cómo le doy el checkin a un profesor?" — se le explicó el mecanismo real (fichaje automático al loguear, T-20260826-007). Usuario respondió con un pedido concreto: "habria que agregar un boton para que aparezca el modal y se pueda registrar el profesor". Se identificó el motivo real detrás del pedido antes de implementar: `/checkin` es un dispositivo compartido de mostrador (recepción logueada, no el profesor), así que el fichaje automático-por-login nunca dispara para un profesor que solo pasa por el mostrador — gap no detectado hasta esta conversación.
- Confirmado con el usuario vía `AskUserQuestion` antes de tocar código: el botón/modal va en `/checkin` (no en Equipo), alcance limitado a Profesores (no todo el staff).
- Implementado sin backend nuevo de fondo — reusa exactamente los mismos endpoints owner+staff que ya usaba el toggle manual de "Limpieza" en Equipo (`app/api/v1/staff/[id]/attendance/**`, T-20260825-005). Único cambio de API: `GET /api/v1/staff/instructors` (creado en T-20260827-007) ganó un campo `openAttendanceId` vía subquery correlacionada, mismo patrón que ya usa el roster owner-only. Nuevo: `features/checkin-page/components/instructor-attendance-dialog.tsx` — modal con la lista de profesores, estado "Fichado"/no fichado, y un botón por fila que llama al mismo POST/PATCH de siempre. Botón "Fichar profesor" agregado al header de `/checkin` (la página no tenía fila de header todavía).
- `tsc`/`eslint`/detector: limpios. `test:unit`: 49/49 — sin migración, la columna que se agregó es calculada (subquery), no una columna real nueva.
- Con esto, un profesor que nunca loguea personalmente en el dispositivo del mostrador igual puede quedar fichado — recepción lo hace por él desde el mismo lugar donde ya atiende socios.

## Notas adicionales
- El indexador built-in es regex-based, no AST completo — imports dinámicos o alias no relativos pueden no resolverse en `references`/`referenced_by`.
- `AGENTS.md` y `CLAUDE.md` en la raíz del repo estaban borrados en el working tree (sin commitear) al momento de instalar GRAPH; se recrearon desde el template del plugin porque el flujo de instalación los trata como "no encontrados". Si esa eliminación había sido intencional, revisar el contenido actual de ambos archivos.
