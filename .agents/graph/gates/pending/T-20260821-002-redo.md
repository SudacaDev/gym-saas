# Gate: T-20260821-002 (redo) — Planes: tabla → grilla de cards

- severity: medium (`edit_non_prod_file` en `gates/policy.yml`)
- approval_mode: async (no bloquea el hilo principal, queda registrado)
- created: 2026-08-21

## Qué se intenta

Reemplazar la tabla de `features/plans-page/index.tsx` por una grilla de cards, sin cambiar comportamiento (mismo fetch, mismo alta/edición/borrado vía `PlanFormDialog`), dentro del lenguaje visual de `DESIGN.md`. Ya se hizo una vez esta sesión (T-20260821-002 original) pero fue ejecutada fuera de proceso — sin `#run` ni este gate. El usuario pidió explícitamente revertirla y volver a correrla bien; este archivo es ese paso que faltó la primera vez.

## Por qué requiere revisión

Cambio de UI en código no productivo — reversible, sin tocar datos ni servicios externos. Severidad `medium` según `default_severity_by_action.edit_non_prod_file` en `policy.yml`.

## Opciones consideradas

Ninguna alternativa real — es la misma implementación ya validada (tsc limpio) la primera vez; este gate es puramente para dejar el rastro de proceso que faltó, no para reabrir el diseño.

## Estado

Aprobación async — el usuario ya autorizó explícitamente "revertirla y re-correrla bien" en conversación (2026-08-21). Procediendo a implementar.
