# Gate: T-20260826-003 — Reducto: resolver solo el subgrafo relevante de la tarea en curso durante `#run`

- severity: medium (`edit_non_prod_file` en `gates/policy.yml`)
- approval_mode: async
- created: 2026-08-26

## Qué se hizo

`AGENTS.md`, paso 8 (`#run`), gana una frase al final:

> Antes de tocar código en cada tarea, resolvé solo el subgrafo relevante con Reducto: si la tarea trae metadata `path:`, corré `reducto query "<esos paths>" --resolve --view skeleton`; si todavía no hay `path:` (tarea recién creada, sin exploración previa), usá las keywords del título de la tarea como query. No corras `--resolve` sobre el grafo entero ni por curiosidad fuera del alcance de la tarea — el objetivo es que `reducto context` refleje qué porción real del proyecto tocó cada tarea, no maximizar cobertura del caché.

No se tocó ningún otro paso ni se renumeró nada.

## Por qué requiere revisión

- Cambia un paso del protocolo `#run` que rige toda ejecución de tareas futura — vale la pena una confirmación explícita antes de que el próximo `#run` lo aplique de punta a punta.
- Es una instrucción de proceso (afecta *cómo* los agentes exploran, no qué código se escribe), category distinta a los cambios de convención de código de T-20260826-001/002 — clasificada igual (`medium`/`edit_non_prod_file`) porque es un archivo no productivo, reversible con un revert simple, sin efecto en runtime ni en datos.
- Se evaluó clasificarlo como `edit_graph_config` (`high`, reservado en `policy.yml` para `circuit-breaker.yml`/`policy.yml`) pero `AGENTS.md` es el bridge/protocolo, no un archivo de enforcement — se mantuvo `medium` por consistencia con el precedente de T-20260824-001 (edición de `AGENTS.md`... en realidad ese fue `owner-nav.tsx`; no hay precedente previo de editar `AGENTS.md` mismo, señalado acá por transparencia).

## Verificación

- Diff de una sola frase agregada al final del paso 8; los pasos 9, 10 y 11 no cambiaron de texto ni de número (se confirmó a mano que las referencias cruzadas existentes — "`AGENTS.md` #11" en el gate de T-20260826-002 — siguen apuntando al paso correcto).
- No aplica `tsc`/`eslint`/`test:unit` (no es código de la app).

## Gaps conocidos

- La instrucción asume que `reducto query --resolve` acepta una lista de paths como query string — no se verificó contra el `--help` real de `reducto query` si soporta filtrar por path de forma nativa o si hay que pasarlo como texto libre (mismo mecanismo que hoy usa para keywords). Si `reducto query` no soporta paths directamente, el fallback a keywords del título sigue siendo válido, pero la parte de "usar `path:` cuando existe" pierde precisión hasta confirmarlo.
- No se corrió `reducto context` después de este cambio para verificar que efectivamente empiece a subir `Tokens saved` — recién se va a poder observar en la próxima tarea real ejecutada vía `#run`.

## Aprobación

- [ ] Aprobado por:
- [ ] Fecha:
- Sin comando post-aprobación pendiente — no hay migración ni cambio de datos reales en esta tarea.
