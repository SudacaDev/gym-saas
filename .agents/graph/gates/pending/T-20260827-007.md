# Gate: T-20260827-007 — aplicar migración `class_schedules.instructor_id` a la base real

- severity: high
- approval_mode: sync (bloqueante — no ejecutar sin aprobación explícita acá)
- requires_named_approver: true
- created: 2026-08-27

## Qué intentó

Ya se hizo, es local y no toca la base real (revertible con `git restore`/`git clean` si hiciera falta):

- `db/schema/class-schedules.ts` — columna nueva `instructorId` (uuid, nullable, FK a `staff_members.id` con `onDelete: "set null"`). Reversa la decisión original de v1 de no tener concepto de instructor (ver comentario en el propio archivo) — pedido explícito del usuario esta sesión: "el profesor si quiere ver su clase... puede cargar su horario y ver cuantas personas hay anotadas en la clase".
- `db/schema/relations.ts` — relación `instructor` en `classSchedulesRelations`.
- `lib/validations/schedule.schema.ts` — `instructorId` opcional/nullable en `scheduleSchema`.
- `lib/schedules/resolve-instructor-id.ts` (nuevo) — un profesor siempre queda auto-asignado a sí mismo en cualquier escritura (POST/PATCH), ignorando lo que mande el cliente; owner/administrativo pueden asignar a cualquiera vía un picker nuevo en `ScheduleFormDialog`. Decisión confirmada con el usuario vía `AskUserQuestion`.
- `app/api/v1/schedules/route.ts` (POST) y `[id]/route.ts` (PATCH) — usan el resolver de arriba.
- `app/api/v1/staff/instructors/route.ts` (nuevo) — endpoint mínimo `{id, firstName, lastName}` por instructor, deliberadamente más angosto que `GET /api/v1/staff` (que es owner-only por decisión previa, gate `T-20260825-001`) — no expone email/DNI/turno, nada que esa restricción haya buscado ocultar.
- `features/schedules-page/**` — picker de instructor en el form (oculto y reemplazado por una nota estática "Vos (Nombre)" cuando quien mira es el propio profesor), filtro "Mis clases" en `/schedules` (las 3 vistas), reutiliza el diálogo de reservas ya existente (`ClassOccurrenceDialog`, de T-011) para ver cuántos hay anotados — sin pantalla nueva.
- `db/migrations/0017_stormy_felicia_hardy.sql` — generado con `drizzle-kit generate` (sin conexión a DB, solo diff de schema local).
- `npx tsc --noEmit`, `npx eslint .`: limpios. Detector de `impeccable`: limpio (`[]`) sobre todo lo tocado.
- `npm run test:unit`: 34/34 verde + 15 skipped en `rls-policies.test.ts` — **ese archivo falla al insertar contra `class_schedules` porque la columna `instructor_id` todavía no existe en la base real** (`PostgresError: column "instructor_id" of relation "class_schedules" does not exist"`), exactamente el estado esperado antes de aplicar esta migración, no una regresión.

Falta ejecutar: `npm run db:migrate` (corre `drizzle-kit migrate` contra `DATABASE_TRUSTED_URL` — la base real de este proyecto — y después `scripts/apply-policies.ts`, que reaplica todas las policies existentes, no solo las nuevas; no hace falta policy nueva para esta migración, es solo una columna en una tabla que ya tiene RLS).

## Contexto cruzado con otra sesión (importante para decidir)

Una sesión concurrente resolvió hoy T-20260827-006 ("mostrar qué profesor está activo" en `/checkin`) y, para **ese** pedido puntual, el usuario confirmó explícitamente NO agregar columna de instructor a `class_schedules` — eligió en cambio "instructores fichados ahora, sin vínculo a la clase" (ver `tasks.md`, T-20260827-006). Esa decisión fue para un widget distinto (mostrar quién está fichado en este momento) y no entra en conflicto con lo que se pidió acá — acá el usuario pidió explícitamente lo contrario ("el profesor... puede cargar su horario", confirmado vía `AskUserQuestion` en esta misma conversación) para un problema distinto (que el profesor vea y cargue SU clase, con el cupo anotado). Se deja documentado para que quien apruebe este gate tenga el contexto completo — no es un cambio de opinión del usuario, son dos pedidos distintos resueltos de forma consistente con lo que cada uno pidió.

## Por qué requiere revisión

- Es una migración de schema (`ALTER TABLE ... ADD COLUMN`, `ADD CONSTRAINT ... FOREIGN KEY`) contra la base de datos real del proyecto.
- No hay entorno de staging separado en este proyecto — este es el único lugar donde se puede aplicar.
- `scripts/apply-policies.ts` reaplica todas las policies existentes cada vez (idempotentes por diseño), no solo las de esta tarea.

## SQL que se aplicaría (`db/migrations/0017_stormy_felicia_hardy.sql`)

```sql
ALTER TABLE "class_schedules" ADD COLUMN "instructor_id" uuid;--> statement-breakpoint
ALTER TABLE "class_schedules" ADD CONSTRAINT "class_schedules_instructor_id_staff_members_id_fk" FOREIGN KEY ("instructor_id") REFERENCES "public"."staff_members"("id") ON DELETE set null ON UPDATE no action;
```

Puramente aditivo — no borra ni modifica ninguna tabla, columna o política existente. Nullable, así que no rompe ninguna fila existente de `class_schedules`.

## Aprobación

- [x] Aprobado por: SudacaDev (aprobación explícita en conversación: "si lo apruebo", tras reportar que `/schedules` había dejado de cargar por el mismatch schema/DB — exactamente el motivo que este gate anticipaba)
- [x] Fecha: 2026-08-27
- Comando corrido: `npm run db:migrate` — `drizzle-kit migrate` aplicó la migración sin errores, `apply-policies.ts` reaplicó las 11 policies existentes (notices esperados de `IF EXISTS`). Verificado contra la base real con una query directa a `information_schema.columns`: `class_schedules.instructor_id` existe (`uuid`, `nullable`), y el FK `class_schedules_instructor_id_staff_members_id_fk` está presente. `npm run test:unit`: 49/49 verde (antes: `rls-policies.test.ts` fallaba por la columna faltante, ahora pasa).

**Nota de proceso:** este archivo se deja resuelto acá mismo, en `gates/pending/`, en vez de moverse a `gates/approved/` — `roles/registry.yml` establece como invariante que ningún rol automatizado escribe ahí. El estado real de ejecución vive en `sessions/tasks.md` (T-20260827-007) y `sessions/progress.md`.
